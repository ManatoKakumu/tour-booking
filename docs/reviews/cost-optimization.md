## 2026-09-03 設計レビュー

## レビュー対象

- 対象サービス: コスト関連(`cost-optimization.md`)
- 対応Step: Step10(Savings Plans/コスト最適化)
- レビュー日: 2026-09-03

## 総評

壁打ちを通じて、Savings Plans/Budgets/Cost Anomaly Detectionそれぞれの採否とその理由が一貫した論理で整理されており、設計レビューとしては合格とし、構築フェーズ(Budgetsの実装)に進んで問題ありません。

今回最も評価したいのは、**Budgetsの金額設定を巡るやり取りで、Claude側の指摘の誤りを本人が実務的な理屈で正しく退けられた**ことです。当初Claudeは「予算額は他レイヤーの積算コストと整合させるべき」と指摘しましたが、本人は「予算は想定コストの実額に合わせるものではなく、組織が許容できる上限として独立に定めるべきもので、実額に近づけすぎるとアラートが早期警戒として機能しなくなる」と反論し、これは実務的により正確な理解でした。レビュー側の指摘を鵜呑みにせず、目的(早期警戒)に立ち返って再反論できたのは良い姿勢です。

一方で、その過程で一度、予算額の理由づけとして個別事情に基づく説明を書きかけた場面がありましたが、最終的には「予算は想定コストの実額ではなく、許容できる上限として独立に定めるべきもの」という、より普遍的な原則に基づく理由づけに整理し直せました。ドキュメントの理由づけを、目先の事情ではなく一般化された原則に基づいて磨き直せた良い例です。

もう一点、当初要件・前提の一項目として検討していた「コスト配分タグ戦略(Layer/BusinessSegmentの2軸)」は、壁打ちの途中で本人が「B/C間のコストの偏りを気にする具体的な理由が今は無い」と判断し、スコープから外しました。これはElastiCache不採用時と同じ「根拠のない先回りの投資はしない」という判断パターンの一貫した適用であり、妥当な判断です。

## 観点別レビュー

### 設計

- Savings Plans(1年以上のコミットが前提だが現状は想定していない)、Budgets(採用、任意タイミングでの早期検知)、Cost Anomaly Detection(履歴データが無く機械学習の前提が成立しない)の3つの採否が、それぞれ独立した根拠に基づいて整理されている
- BudgetsとCost Anomaly Detectionは「予期しない出費に早く気づく」という目的こそ似ているが、「閾値を自分で決めるか、機械学習に委ねるか」という手段の違いで役割が分かれている、という整理が(明示的な比較文はないものの)2つの要件・前提の記述を通じて読み取れる

### セキュリティ

- Budgetsの通知先について、「今回は自分のメアドに送信するが、実際はコスト通知を知らせるべき相手を適切に考え、その人たちだけが見れる場所に送信する」と、内部情報であるコストデータの通知経路にもアクセス制御の観点を適用できている

### 可用性

- 通知先が1人のみである状態を「誰かが気づけない事態」というリスクとして捉え、複数人への通知という設計を可用性設計の項目に位置づけている。このプロジェクトが一貫して使ってきた「単一障害点を避ける」という考え方(NAT Gateway、Multi-AZ等)を、インフラではなく運用体制(人間の気づき)にまで応用できている点は良い着眼点

### 運用性

- アラート閾値(実測値50%/80%/100%、予測値100%)の設計にあたり、「アラート検知からどれくらいの時間で対処できるか」「AWSリソースの削減に自組織外の承認が要るか」という、閾値の大小を左右する実務上の要因に触れられている

### 保守性

- タグ戦略をスコープから外したことで、既存の全レイヤー(network-sg-alb・database・compute-b/c等)への横断的な変更を伴わない、影響範囲の小さい変更に留められている

### コスト

- Budgetsの無料枠について、当初「予算の個数(2つまで)」を無料の条件としていたが、AWS公式FAQで「アクション連携の有無」が条件であり、通知のみの予算は個数に関わらず無料であることを確認し、正確な記述に修正できた
- 予算額($18)の位置づけについて、「想定コストの実額ではなく、許容できる上限として独立に定めるべきもの」という説明に修正済み。個別事情に基づいていた当初の理由づけも、より一般化された原則に基づく説明に整理し直されている

### パフォーマンス

- Budgets/Cost Anomaly Detection/Savings Plansはいずれも請求管理機能でVPC外・コンソール/API完結のため、特筆すべき懸念はない

### Terraform化した場合の改善点

- 未実装のため、次回の実装レビューで評価する

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: 早期検知(段階的なアラート閾値)、複数人への通知という考え方を構築前から組み込めている
- セキュリティ: コスト情報という内部情報の通知経路にアクセス制御の観点を適用できている
- 信頼性: 該当なし(通信経路が存在しないため)
- パフォーマンス効率: 該当なし
- コスト最適化: 「予算=許容上限であり想定コストの実額ではない」という整理、Savings Plans/Cost Anomaly Detectionの採否判断いずれも筋が通っている
- 持続可能性: 特筆事項なし

### 実務ならどう設計するか

- 予算(Budget)を想定コストの実額と混同しない、という整理は実務でも重要な観点。レビュー側の指摘であっても鵜呑みにせず、目的に立ち返って再検証できたのは良い実務センス
- ドキュメントの理由づけを、個別事情に基づく説明から、より普遍的な原則(予算=許容上限という考え方)に基づく説明へ磨き直せたのは良い実務センス。社外向けの技術ブログ・提案書等の文書作成でも使える技術

## 理解できていること

- BudgetsとCost Anomaly Detectionの役割の違い(閾値を自分で決めるか、機械学習の学習データに委ねるか)
- 予算額は想定コストの実額ではなく、組織が許容できる上限として独立に定めるべきという考え方
- ElastiCache不採用時と同じ判断パターン(根拠のない先回りの投資をしない)を、タグ戦略の見送り判断にも一貫して適用できたこと

## 曖昧なこと

- 実際にBudgetsを構築する際の具体的な設定(Cost BudgetかUsage Budgetか、SNS経由かメール直接か)は、構築フェーズで詰める

## 理解できていないこと

- 特になし

## 次のアクション

- [x] 構築フェーズ(Terraform実装)に進んでよい
- [x] Budgetsをどのtfstateレイヤーに配置するか検討する(既存6レイヤーのいずれにも明確に属さないため、新規レイヤーとするか、既存レイヤーに含めるか本人判断)→新規レイヤー`cost-management`に決定
- [x] 実装後、`docs/architecture/README.md`の状態を「完了」に更新する

## 2026-09-04 実装レビュー

## レビュー対象

- 対象: `infra/cost-management/`(新規)
- 対応Step: Step10(Budgets実装)
- レビュー日: 2026-09-04

## 総評

`aws_budgets_budget`1リソースの実装・`apply`・AWS CLIでの実機照合・`destroy`まで完了し、実装レビューとしては合格、`docs/architecture/README.md`の状態を「完了」に更新して問題ありません。

設計・実装を通じて一貫していたのは、**tfstate分割の判断を、このプロジェクトが繰り返し使ってきた基準(変更頻度・ライフサイクル独立性・blast radius・権限境界、および「独立していることは必要条件だが十分条件ではない」という原則)に照らして自力で導けたこと**です。特にADR-002の見直しでは、CloudFront/S3・B側コンピュートの分割理由が「AWSリソースレベルの設計意図」と「Terraform state分割の理由」を混同していることを、レビュー指摘を受けてすぐに切り分け直せました。

## 観点別レビュー

### 設計

- `aws_budgets_budget`単体のシンプルな構成で、設計(`cost-optimization.md`)通りに実装できている
- tfstateレイヤーの新規切り出し(`cost-management`)は、依存先ゼロという点で`route53-acm`の前例と一貫した判断

### セキュリティ

- `notification_email`を`sensitive = true`の変数にし、`terraform.tfvars`(gitignore対象)経由で渡す設計にできている。個人のメールアドレスを公開リポジトリに直書きするリスクを、既存のbackendバケット名保護と同じパターンで回避できた

### 可用性

- 該当なし(design doc記載通り)

### 運用性

- AWS CLI(`describe-budget`/`describe-notifications-for-budget`)による実機照合を実施し、`BudgetLimit`・`TimeUnit`・4件の通知設定いずれも設計通りであることを確認できた
- 実際の`ForecastedSpend`($10.711)が算出されており、利用実績が少ない段階でもAWS側の予測機能自体は動作することを実機で確認できた(精度は別として、仕組みが機能することの確認としては十分)

### 保守性

- コード量が少なく、シンプルな構成に留められている。他レイヤーからのコピペで紛れ込んでいた不要な`tfstate_bucket`変数も、レビュー指摘を受けて削除済み

### コスト

- `aws_budgets_budget_action`(アクション連携)を使わない、通知のみの構成のため無料。設計時点の見積もりと実際の構築内容に齟齬がない

### パフォーマンス

- 該当なし

### Terraform化した場合の改善点

- 特になし(今回の実装がそのままTerraform化されている)

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: AWS CLIでの実機照合を実施し、設計と実装の一致を確認できている
- セキュリティ: 通知先個人情報のsensitive変数化・tfvars分離ができている
- 信頼性: 該当なし
- パフォーマンス効率: 該当なし
- コスト最適化: 通知のみの構成で無料枠内に収まる設計を維持できた
- 持続可能性: 特筆事項なし

### 実務ならどう設計するか

- 実際のプロダクトでは、閾値超過時の通知先としてSlack連携やPagerDuty等の運用ツールとの統合も検討されうるが、今回のスコープ(個人開発、メール通知のみ)では過剰投資であり、見送りは妥当

## 理解できていること

- `aws_budgets_budget`の`notification`ブロックの構造(`comparison_operator`・`threshold`・`threshold_type`・`notification_type`の組み合わせ)
- sensitive変数+gitignore対象のtfvarsで機密情報を扱うパターン
- tfstate分割の判断基準(4軸、および「独立していることは必要条件だが十分条件ではない」という原則)を、実際のADR記載の誤りを見つけて修正する形で運用できたこと

## 曖昧なこと

- 実際に閾値を超えた場合にメール通知が届くかどうかは、今回は実データが無いため未検証(設定自体はAWS CLIで正しく登録されていることを確認済み)

## 理解できていないこと

- 特になし

## 次のアクション

- [x] `docs/architecture/README.md`のコスト関連の状態を「完了」に更新する
- [ ] ロードマップ全体(Step1〜10)が完了。次のセッションは、持ち越し済みの「コンテナのベースイメージ脆弱性対応」、または今回発見した「ECRライフサイクル用LambdaのB/C権限分離」のいずれか(本人判断)
