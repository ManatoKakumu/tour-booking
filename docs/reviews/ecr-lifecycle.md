## 2026-08-25 設計レビュー

## レビュー対象

- 対象サービス: ECRライフサイクル(`ecr-lifecycle.md`)
- 対応Step: ECRライフサイクルポリシー・ヘルスチェック連動タグ付け(Lambda+EventBridge、プロジェクト初のLambda)
- レビュー日: 2026-08-25

## 総評

良い出来です。特に、「ビルド成功/失敗」ではなく「ECSへのデプロイ成功/失敗」という、ドキュメントのタイトル(ヘルスチェック連動タグ付け)が本来意図している基準に、指摘を受けて正確に言葉を揃え直せた点が良い出発点になっています。

セキュリティ設計では、EventBridgeとLambdaそれぞれの実際の役割(EventBridgeは受動的にイベントを受け取って中継するだけ、実際にAWS APIを呼び出すのはLambda)を取り違えた状態から、正しく権限を再配置できました。可用性設計でも、「EventBridgeのDLQ」と「Lambdaの送信先(Destination)」という、似ているが対象とする失敗の層が異なる2つの仕組みを混同しかけましたが、最終的に「Lambda関数内部の処理失敗」という本当に心配すべき対象に対して、Destination→SNS→(SQS+通知)という妥当な構成にたどり着けています。ECRの`IMMUTABLE`設定と「タグの付け替え」が両立するかどうかを、既存の決定事項(CI/CD設計時のタグ運用方針)まで遡って確認した点も、他レイヤーとの一貫性を保つ姿勢として評価できます。

コスト見積もりのGB秒計算(128MBをGB換算する際に1024ではなく1000で割ってしまう)は、Fargateのコスト見積もり時と全く同じ種類のミスが再発しましたが、結論には影響しない範囲で、指摘後すぐに正しく修正できています。同じパターンのミスだと自分でも認識しておくと良さそうです。

設計レビューとしては合格とし、構築フェーズ(Terraform実装、Lambda関数の実装)に進んで問題ありません。

## 観点別レビュー

### 設計

- 3種類のタグ(`稼働中`/`成功`/`失敗`)と、それぞれのライフサイクルポリシー(保持なし/90日/30日)の使い分けが、要件・前提(ロールバック用途・調査用途)と一貫している
- `稼働中`タグの付け替えを、ECRの`IMMUTABLE`設定と矛盾しない「タグ削除→タグ追加」の2段階操作として正しく整理できている
- 通信経路(`ECS(デプロイサーキットブレーカー) → EventBridge → Lambda → ECR`)が、実際にデータ・制御がどう流れるかを過不足なく表現している

### セキュリティ

- EventBridge・Lambdaそれぞれに必要な権限だけを個別に切り分けられている(EventBridgeは`lambda:InvokeFunction`のみ、ECS読み取り・ECR操作はLambda側)
- `ecr:GetAuthorizationToken`(Dockerレジストリとしての認証用)を、ECRの管理API(`PutImage`等、IAM認証のみで完結)には不要と正しく判断し、削除できている
- リソースをARNで絞り込む姿勢(ECS4サービス・ECR4リポジトリを個別指定)が、既存レイヤー(compute.md等)の最小権限の原則と一貫している

### 可用性

- Lambdaの予約済み同時実行数(4)の設定理由(4サービス分の並列実行を確保しつつ、将来の他Lambda追加時の圧迫を防ぐ)が明確
- Lambda関数内部の処理失敗時に、送信先(Destination)→SNS→(SQS保存+通知)という構成で、失敗を握りつぶさない設計になっている
- **軽微な指摘**: SQSの保持期間(最大14日)の制約は、SNSによる即時通知とセットであることを前提に許容する、という判断がされているが、この理由(なぜ14日で問題ないか)自体はドキュメント本文にまだ明記されていない。次のアクションに残す

### 運用性

- 90日・30日という保持期間の数値に根拠がないことを正直に明記した上で、その決定理由(平均デプロイ間隔・調査にかかる時間より少し長め)を添えており、後から見直す際の材料になっている
- コスト見積もりで、各コンポーネントについて「なぜ無料枠に収まるか」の計算過程を残しており、前提(デプロイ頻度)が変わった際に再計算しやすい

### 保守性

- IAMロールの権限を「EventBridge」「Lambda」で明確に分けて記載しており、後から見てどちらの権限か迷わない
- タグの命名(`稼働中`/`成功`/`失敗`)が日本語で統一されており、実装時にも一貫した命名で運用できる見込み

### コスト

- EventBridge・Lambda・SNS・SQSいずれも、現在の低頻度なデプロイ頻度を前提に無料枠に収まる計算ができている
- Lambdaの実行時間見積もり(GB秒)で、128MBをGB換算する際の誤り(1024ではなく1000で割っていた)があったが、指摘後正しく修正されている。結論($0)自体は変わらない範囲のミスだった

### パフォーマンス

- 特筆事項なし(イベント駆動・低頻度実行の性質上、パフォーマンス上の懸念は小さい)

### Terraform化した場合の改善点

- Lambda関数のランタイム・タイムアウト設定は、コスト見積もりで置いた仮定(128MB・60秒)と整合させて実装する
- EventBridgeルールのイベントパターン(`source: aws.ecs`、`detail-type`の具体的な値)は、実装時にECSが実際に発行するイベントのdetail-typeを確認しながら詰める必要がある

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: 失敗時の通知・保存の仕組みがあり、問題発生に気づける設計になっている
- セキュリティ: EventBridge/Lambdaそれぞれに最小権限が正しく切り分けられている
- 信頼性: `稼働中`イメージを絶対に削除しない設計、失敗時の握りつぶし防止の両方が担保されている
- パフォーマンス効率: 低頻度・軽量処理に対して過不足のない構成(Lambda最小メモリ等)
- コスト最適化: 全コンポーネントが無料枠に収まる計算がされており、不要なコストが発生しない
- 持続可能性: 特筆事項なし

### 実務ならどう設計するか

- 「タグの意味(稼働中/成功/失敗)」を明確に定義してからライフサイクルポリシーを設計する進め方は、実務でもそのまま通用する。あいまいな基準でライフサイクルポリシーだけ先に作ると、後から「このタグは何のためにあるのか」が分からなくなりがち
- ECRの`IMMUTABLE`設定のような、他のレイヤーで決めた既存の制約を横断的に確認する姿勢は、実務のレビューでも高く評価されるポイント

## 理解できていること

- ECRの`IMMUTABLE`タグ設定が「上書き」を防ぐものであり、「削除→追加」の2段階操作は妨げないという技術的な仕組み
- ECRの2つの「入口」(Dockerレジストリとしての認証が必要な経路と、AWS管理APIとしてIAM認証のみで完結する経路)の違い
- EventBridgeのDLQ(配信層の失敗)とLambdaのDestination(関数内部処理層の失敗)が、対象とする失敗の層が異なるという整理
- SNSのファンアウト(1つのトピックから複数の購読者へ配信)を使って、「保存」と「検知」を1つの送信先設定で両立させる考え方

## 曖昧なこと

- SQSの保持期間(14日)がなぜ今回のケースで許容できるか(SNSによる即時通知とセットであること)の理由が、本人の理解としては会話の中で到達しているが、ドキュメント本文にはまだ明記されていない

## 理解できていないこと

- 特になし

## 次のアクション

- [ ] `ecr-lifecycle.md`の可用性設計に、「SQSの14日保持で問題ない理由(SNS通知とセットであるため)」を一文追記する
- [ ] Terraform実装時、ECSサービス(`aws_ecs_service`)に`deployment_circuit_breaker`(`enable = true`)を追加する(現状のTerraformにはまだ設定されていない)
- [ ] EventBridgeルールのイベントパターン(実際のdetail-type)を、実機で確認しながら詰める
- [ ] 構築(Terraform実装・Lambda関数の実装)に進んでよい

## 2026-08-25 実装レビュー

## レビュー対象

- 対象: `infra/compute-b/`・`infra/compute-c/`(`ecr.tf`のライフサイクルポリシー・`ecs.tf`のサーキットブレーカー・`outputs.tf`新規)、`infra/ecr-lifecycle/`新規(`iam.tf`・`lambda.tf`・`lambda/handler.py`・`eventbridge.tf`・`sns.tf`・`sqs.tf`・`remote_state.tf`等)
- 対応Step: ECRライフサイクルポリシー・ヘルスチェック連動タグ付け(プロジェクト初のLambda)
- レビュー日: 2026-08-25

## 総評

実装の過程で、設計レビューでは見つからなかった実装レベルの不具合を複数発見しましたが、いずれもその場で修正されています。特に、ECRの`IMMUTABLE`設定と両立しない`aws_ecr_lifecycle_policy`の重複作成(1リポジトリに複数ポリシーリソースを割り当てようとしたミス)、Dockerタグの文字種制約(日本語タグ名`稼働中`/`成功`/`失敗`が`apply`時にエラーとして顕在化)、`success`/`fail`タグを固定文字列のまま使い回すと過去のイメージを見失う設計上の欠陥(本人が自ら「嘘をついていることになる」と指摘し、digest/image_tagベースの一意なタグ名に設計を修正)、ロールバック発生時に`describe_services`の「現在の」タスク定義だけでは失敗した方のイメージを正しく特定できない問題(`deploymentId`で個別のデプロイメントを追跡する方式に変更)は、いずれも一度見過ごされたまま提示されかけた実装ですが、指摘後は正確に理解した上で修正できています。

IAMロールの権限漏れ(`ecs:DescribeTaskDefinition`・`sns:Publish`)も、実装中に発見・修正され、最終的に設計ドキュメント側にも反映されました。設計ドキュメントと実装のズレを放置しない、というこのプロジェクトで一貫した姿勢が今回も貫かれています。

動作確認では、初回デプロイ(`current`のみ付与)・2回目以降のデプロイ(`current`→`success-<digest>`の付け替え)・Lambda自体の処理失敗からSNS→SQSへの通知、いずれも実機で確認できました。唯一、ECSのデプロイ失敗(サーキットブレーカー発動)から`fail-`タグ付与までの経路は、実際にサーキットブレーカーを発動させる試行はしたものの、AWS側の内部的な失敗検知間隔により時間内に発動せず、今回は打ち切りとなりました。ロジック自体はコードトレースで妥当性を確認済みのため、実装レビューの合否には影響しませんが、次回以降どこかのタイミングで実機確認する価値はあります。

実装レビューとしては合格とし、mainへのマージに進んで問題ありません。

## 観点別レビュー

### 設計

- 3種類のタグ(`current`/`success-<digest>`/`fail-<image_tag>`)の運用が、ECRの`IMMUTABLE`設定・Dockerのタグ文字種制約と矛盾しない形で実装されている
- 初回デプロイ(`current`タグがまだ存在しない)というエッジケースを、`try/except IndexError`で正しく吸収できている
- ロールバック時の失敗イメージ特定を、サービス全体の「現在」ではなく`deploymentId`単位で追跡する設計に修正できており、設計レビュー時点では見えていなかった実装レベルの正確性が確保されている

### セキュリティ

- EventBridge・Lambdaそれぞれの権限が、実装を通じて過不足なく揃った(`ecs:DescribeTaskDefinition`・`sns:Publish`の後追い追加を含む)
- `ecr:GetAuthorizationToken`(Dockerレジストリ認証用、今回のLambdaには不要)を混入させなかった設計レビュー時点の判断が、実装でもそのまま維持されている

### 可用性

- Lambda送信先(Destination)→SNS→SQS+メール通知の経路を、実際にLambdaを異常系イベントで非同期呼び出しして確認済み(3回リトライ後、`RetriesExhausted`でSNSへ配信)
- ECSデプロイ失敗時の`fail-`タグ付与ロジックは、実機でのサーキットブレーカー発動までは確認しきれなかった(次のアクションに残す)

### 運用性

- 実装中に発見した設計上の欠陥(タグの一意性不足)を、その場しのぎで済ませず、設計ドキュメント本文の記載(`選定理由`のタグ命名規則)まで書き直せている
- コスト見積もりの計算ミス(GB秒換算で1024ではなく1000で割っていた)を、Auto Scalingセッションと同じパターンの間違いだと自覚した上で修正できている

### 保守性

- `add_tag`/`remove_tag`/`get_digest`/`get_deployment_image`と、目的ごとに関数を分割できており、成功/失敗どちらの分岐からも再利用できる構造になっている
- `aws_ecr_lifecycle_policy`を1リポジトリ1リソース・複数ルールにまとめ直した判断(設計レビュー後に発覚したAWS側の制約への対応)が、`compute-b`/`compute-c`両方に一貫して反映されている

### コスト

- 設計時点の見積もり($0)から変更なし。実装内容も見積もり時の前提(128MB・60秒・月100回程度)と乖離していない

### パフォーマンス

- 未計測(実行時間はCloudWatch Logsで778ms〜902ms程度と確認済みで、見積もり時の60秒という保守的な想定を大きく下回っている)

### Terraform化した場合の改善点

- 特筆事項なし。IAMロール・Lambda・EventBridge・SNS/SQSいずれも、既存レイヤーの命名・構造パターンを踏襲できている

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: 実装中に見つかった問題(自分のミス・AWS仕様の制約双方)が、すべて記録に残っている
- セキュリティ: 最小権限の原則が実装完了時点で一貫している
- 信頼性: Lambda処理失敗時の通知・保存経路を実機確認済み。ECS側の失敗経路は確認未完了
- パフォーマンス効率: 実測値が見積もりを大きく下回っており、過剰な設定になっていない
- コスト最適化: 見積もり通り$0で運用できる見込み
- 持続可能性: 特筆事項なし

### 実務ならどう設計するか

- 「タグを使い回すと過去の情報を見失う」という指摘は、実務でも起きがちな「同じラベルを複数の対象に使い回してしまう」典型的なアンチパターン。本人が「嘘をついていることになる」と表現した通り、名前が実体を正しく指さない状態はデバッグ時の混乱に直結するため、実務のレビューでも同様に指摘される類の問題
- ECSデプロイ失敗の実機確認が時間内に完了しなかった点は、実務でも「本番相当の障害シナリオを人工的に再現するコスト」として現実的によくある制約。ロジックのトレース確認で一定の担保をした上で、優先度に応じて後回しにする判断は妥当

## 理解できていること

- ECRの`IMMUTABLE`設定・Dockerのタグ文字種制約という、2つの異なるAWS/Docker側の制約
- タグを「使い回す」ことと「一意に保つ」ことの違い、およびそれがなぜ重要か(過去のイメージを見失わないため)
- ロールバック時、サービス全体の「現在の状態」と「個別のデプロイメント」が別物であること
- Lambdaの非同期呼び出し(リトライ→Destination)と、EventBridge側の配信失敗(DLQ)が別の層の失敗であること

## 曖昧なこと

- 特になし

## 理解できていないこと

- 特になし

## 次のアクション

- [ ] ECSデプロイ失敗(サーキットブレーカー発動)から`fail-`タグ付与までの経路を、いずれかのタイミングで実機確認する
- [ ] Stripe egressプレフィックスリスト同期Lambdaに進んでよい

## 2026-09-23 設計レビュー(ECRライフサイクル用LambdaのB/C権限分離)

## レビュー対象

- 対象サービス: ECRライフサイクル(`ecr-lifecycle.md`)、Lambda・IAMロール・EventBridgeルールの4分割
- 対応Step: 2026-09-04のセッションで発見・保留していた「ECRライフサイクル用LambdaがB/C両方のECSサービス・ECRリポジトリへの権限を1つで持っている」というB2B/B2C分離原則との矛盾の解消
- レビュー日: 2026-09-23

## 総評

良い出来です。単に「1つのLambdaを4つに分ける」という結論だけでなく、「なぜ2つ(B/C単位、DBユーザーと同じ粒度)ではなく4つ(サービス単位、OIDCロールと同じ粒度)なのか」まで、このプロジェクトの既存パターン(DBユーザーは実際にDBへアクセスする主体が2つしかないため2分割、OIDCロールは独立してデプロイされる主体が4つあるため4分割)と比較しながら導き出せている点が、今回のレビューで最も評価できる部分です。単なる横並びの模倣ではなく、粒度が変わる理由まで言語化できています。

最小権限の見直しも、ECSサービス・ECRリポジトリの権限は早い段階で正しく4分割できていましたが、`ecs:DescribeTaskDefinition`だけが`Resource: "*"`のまま見直しから漏れていました。指摘を受けた後、`infra/compute-b/ecs.tf`の`family = each.key`(family名がサービス名と一致する実装済みの事実)を根拠に、正しくtask-definition単位まで絞り込めています。今回の変更の目的(侵害時の被害範囲の最小化)に照らして、見直し対象を漏れなく洗い出す姿勢が最後まで貫けたかどうかが今回の一番のポイントでしたが、最終的には貫けています。

可用性設計の同時実行数の見直しでは、「4サービス分の非ブロッキング」という元の目的が実は関数分割自体で構造的に解消されること、そして「各1」にすることで新たに得られる効果(同一サービス内での同時実行によるタグ競合の防止)を、私が提示した説明より正確に言い換えられていた点も良かったです。

設計レビューとしては合格とし、Terraform実装(4つのLambda関数・IAMロール・EventBridgeルールへの分割)に進んで問題ありません。

## 観点別レビュー

### 設計

- 「1つを4つに分ける」理由と「2つではなく4つにする」理由を分けて記載できており、将来この文書だけを読んだ人が「DBは2分割なのになぜここは4分割か」と疑問を持たずに済む
- 通信経路(`ECS → EventBridge → Lambda → ECR`)が4サービス分並列に存在する構成であることを明記できている

### セキュリティ

- ECS読み取り系アクション・ECR書き込み系アクションともに、サービス単位のARNまで絞り込めている
- `ecs:DescribeTaskDefinition`の`Resource: "*"`という見直し漏れを、実装済みのTerraformコード(family名の実態)を根拠に修正できた。最小権限の見直しは「新しく作る権限」だけでなく「既存のまま残っている権限」も同じ基準で洗い出す必要があることを実践できている
- EventBridgeのルーティングを`resources`フィールド(ECSサービスのARN)で絞り込む設計により、意図しないLambdaへの誤配信(≒無関係な権限エラーの多発)を防いでいる

### 可用性

- 同時実行数を「合計4」から「各1」に変更した際、単なる数値の言い換えではなく、元の目的(サービス間の非ブロッキング)が関数分割自体で構造的に解消済みであることと、新たに得られる効果(同一サービス内の競合防止)を区別して説明できている
- SNS/SQSを分割せず共有のままにした判断(書き込み権限を持つリソースではないため、今回の分離の対象外)も、ALBを論理1つのままにした過去の判断と同じ考え方で一貫している

### 運用性

- **指摘**: Lambda関数・IAMロール・EventBridgeルールがいずれも1つから4つに増えることで、Terraformの記述量・変更対象が単純に4倍になる。`infra/compute-b/ecs.tf`で使われている`for_each`のような繰り返しパターンが使えないか、実装時に検討する余地がある。現時点のドキュメントにはこの実装方針への言及がないため、次のアクションとして残す

### 保守性

- 「なぜ4分割にしたか」の判断根拠が選定理由に明文化されており、後から読み返した際に疑問が生まれにくい

### コスト

- 4分割後も合計の見積もり($0)は変わらない。無料枠の判定がアカウント単位であり、関数を分けても実行回数・実行時間の合計自体は変化しないことを正しく確認できている

### パフォーマンス

- 特筆事項なし

### Terraform化した場合の改善点

- `front-b`/`api-b`/`front-c`/`api-c`という4つの文字列に対する繰り返しパターンなので、`for_each`でLambda・IAMロール・EventBridgeルールをDRYに実装できないか検討する。ただしIAMロールの中身(参照するECSサービスARN・ECRリポジトリARNがサービスごとに異なる)をどう`for_each`のキー・値に落とし込むかは、実装時に整理が必要

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: Terraformコード量の増加をどう管理するか(for_each等)は実装時の課題として残る
- セキュリティ: 今回のセッションの主目的(最小権限をサービス単位まで徹底する)を達成
- 信頼性: 既存の失敗検知の仕組み(Destination→SNS→SQS)はそのまま維持
- パフォーマンス効率: 変更なし
- コスト最適化: 変更なし($0)
- 持続可能性: 特筆事項なし

### 実務ならどう設計するか

- 権限を絞り込む作業では、今回のように新規追加した権限だけでなく、既存のまま残っている権限(`DescribeTaskDefinition`)も同じ基準で見直し対象に含める必要がある。変更の目的(被害範囲の最小化)に照らして「このアクションは対象か」を全アクションについて逐一確認する姿勢は、実務のセキュリティレビューでもそのまま求められる
- 既存の類似パターン(DBユーザーの2分割、OIDCロールの4分割)と比較し、なぜ今回は違う粒度になるのかを言語化する進め方は、意思決定の一貫性を第三者に説明する上で実務でも評価されるポイント

## 理解できていること

- IAMロールとLambda関数が1:1で対応する制約(1つのLambda関数には1つの実行ロールしか持たせられない)
- 「アクセス・デプロイする主体の数」という同じ判断基準を、DB(2)とECR/ECS(4)という異なる粒度の実例に正しく当てはめられること
- 権限を絞り込む際、対象アクションのリソースタイプ(ECSのtask-definitionとECRリポジトリは別物)を区別する必要があること
- EventBridgeのイベントパターンで、`resources`フィールドを使って発生元のリソース単位にルーティングを絞り込めること

## 曖昧なこと

- 特になし(今回のセッション内で解消済み)

## 理解できていないこと

- 特になし

## 次のアクション

- [ ] Terraform実装(`infra/ecr-lifecycle/`のLambda関数・IAMロール・EventBridgeルールをそれぞれ4つに分割)に進む。`for_each`によるDRY化を検討する
- [ ] `docs/architecture/README.md`のECRライフサイクルの状態を、Terraform実装・実機確認が終わるまで「構築中」に更新する(実装完了後、実装レビュー合格をもって「完了」に戻す)
- [ ] `overall.drawio.png`のECR/Lambda部分が、この4分割の詳細まで反映すべきか確認する
- [ ] 実装完了後、実装レビューを行う

## 2026-09-26 実装レビュー(ECRライフサイクル用LambdaのB/C権限分離)

## レビュー対象

- 対象: `infra/ecr-lifecycle/`の`iam.tf`・`lambda.tf`・`eventbridge.tf`(`sns.tf`・`sqs.tf`・`lambda/handler.py`は変更なし)
- 対応Step: 2026-09-23設計レビュー合格分のTerraform実装
- レビュー日: 2026-09-26
- 確認範囲: `terraform validate`合格まで。`plan`はcompute-b/cのremote stateが未構築のため実行できず、`apply`・実機確認はアプリケーション実装後の再構築時にまとめて行う方針(本人判断、理由は下記)

## 総評

設計通りに実装できており、コード上の指摘事項は最終的にありません。レビューの過程で見つかった実装上の誤りは3点(いずれも修正済み)です。

- `for_each`の繰り返しの元をロールにしたことで`each.value`の意味が変わり、`Resource`にロールのオブジェクトが入る形になっていた(ECS・ECRのARN絞り込みが機能しない)
- `basic_execution`が`for_each = local...`に変えた後も`each.value.name`のままで、参照が壊れていた
- `eventbridge.tf`のルールを4つに増やしたものの、`event_pattern`に`resources`の絞り込みが入っておらず、4つとも同じ条件になっていた(1回のデプロイイベントで4つのLambdaが全部起動する状態)

Terraformの`for_each`に慣れていない状態からの着手でしたが、「繰り返しの元を全リソースで`local.ecs_service_arns`に統一し、`each.key`で同名の別リソースを引く」というパターンを理解した後は、`lambda.tf`・`eventbridge.tf`を指摘なしで書けています。

実機確認は見送りとします。理由は、確認のためにnetwork-sg-alb・database・compute-b/cの再構築(橋渡し手順・イメージpush含む)が必要で、アプリケーション実装後にどのみち再構築するため、ここで二重に行うより、アプリ完成後に現実的なデプロイ成功・失敗シナリオでまとめて確認する方が効率的かつ確認の質も高いためです。実装レビューとしては合格とし、mainへのマージに進んで問題ありません。ただし、下記の未確認事項が残っています。

## 観点別レビュー

### 設計

- 設計ドキュメント通り、Lambda・IAMロール・EventBridgeルールがfront-b/api-b/front-c/api-cの4セットに分かれている
- SNS/SQSは共有のまま、各Lambdaの`event_invoke_config`から同じSNSトピックを送信先にしており、設計判断と一致している

### セキュリティ

- 各ロールのECS読み取り・ECR書き込み・タスク定義参照が、自サービス分のARN1つに絞られている(`each.value`・`local.ecr_repository_arns[each.key]`・`task-definition/${each.key}:*`)
- 信頼ポリシーの`aws:SourceArn`が関数名(`ecr-tagging-lambda-${each.key}`)と一致しており、他の関数からのAssumeRoleを防げる構造になっている
- **未確認**: `ecs:DescribeTaskDefinition`をタスク定義ARNで絞る書き方が、AWS側で実際に受け付けられるか。ARNの形式自体は公式ドキュメントに記載があるが、アクション単体がリソース指定に対応しているかは一次情報で確認できていない。非対応の場合、Lambdaの成功時処理(`describe_task_definition`)が`AccessDenied`になるため、実機確認で必ず確かめる

### 可用性

- `reserved_concurrent_executions = 1`が各関数に設定され、設計通り(1関数につき1、合計4)
- 4つのルールの`resources`絞り込みにより、1つのデプロイイベントが1つのLambdaだけを起動する構造になっている

### 運用性

- `for_each`で4セットを1つの定義にまとめられており、サービスが増減した場合もlocalを直せば追従する。設計レビューで挙げたDRY化の指摘に対応できている

### 保守性

- `iam.tf`の`locals`(`ecs_service_arns`・`ecr_repository_arns`)を`lambda.tf`・`eventbridge.tf`でも共有しており、キーの一元管理ができている
- 関数名がIAMの信頼ポリシーと`lambda.tf`で手書きの文字列として重複している(`ecr-tagging-lambda-${each.key}`)。現状は一致しているが、片方だけ変更すると壊れるため、将来変更する際は両方を確認する必要がある

### コスト

- 設計時点の見積もり($0)から変更なし

### パフォーマンス

- 変更なし

### Terraform化した場合の改善点

- 関数名の重複(上記)を、`locals`に関数名のmapを定義して両方から参照する形にすれば、片方だけ変更する事故を防げる。今回の規模では必須ではなく、任意の改善

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: 実機確認を後回しにする判断とその理由・未確認事項を記録に残せている
- セキュリティ: サービス単位の最小権限が実装として揃った。`DescribeTaskDefinition`の絞り込みのみ実機未確認
- 信頼性: 既存の失敗検知(Destination→SNS→SQS)は変更なし。ただし4分割後の動作確認は未実施
- パフォーマンス効率: 変更なし
- コスト最適化: 変更なし($0)
- 持続可能性: 特筆事項なし

### 実務ならどう設計するか

- 実務でも、`for_each`で複数リソースを繰り返す際、「繰り返しの元を何にするか」で`each.value`の意味が変わることは典型的なつまずきどころ。繰り返しの元を1つのlocalに統一し、`each.key`で他のリソースを引く書き方は、読みやすさの面でも標準的なパターン
- 実機確認のコストが高い場合に、確認をより現実的なシナリオが用意できるタイミングまで後ろ倒しにする判断自体は実務でも行われる。ただし、未確認事項を明文化して次のアクションに残すことが前提

## 理解できていること

- `for_each`で`each.key`が要素の名前、`each.value`が要素の中身であり、`[each.key]`で別のリソース・mapから同名のものを引ける
- Lambda関数1つにつきIAMロール1つ、`reserved_concurrent_executions`は関数単位で効く
- EventBridgeの`event_pattern`の`resources`で、発生元リソース単位にルールを絞り込める

## 曖昧なこと

- `for_each`の「繰り返しの元」を何にすべきかの選び方(今回はlocalに統一する形で整理した。他のレイヤーで同様の書き方をする際に再確認する)

## 理解できていないこと

- 特になし

## 次のアクション

- [ ] アプリケーション実装後にcompute層を再構築する際、次を実機確認する: (1) 4分割後のLambdaが、対応するサービスのデプロイイベントだけで起動し、タグ付け替え(`current`→`success-<digest>`)が成功する、(2) `ecs:DescribeTaskDefinition`のタスク定義ARN絞り込みが`AccessDenied`にならない(なる場合は`"*"`に戻す判断が必要)、(3) 持ち越し中の、ECSデプロイ失敗(サーキットブレーカー発動)から`fail-`タグ付与までの経路
- [ ] `overall.drawio.png`のECR/Lambda部分が、この4分割の詳細まで反映すべきか確認する(任意)
- [ ] mainへのマージに進んでよい
