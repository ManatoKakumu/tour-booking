## 2026-08-14 設計レビュー

## レビュー対象

- 対象サービス: ネットワーク(`network.md`) / セキュリティグループ(`security-group.md`) / ALB(`alb.md`) / Cognito(`cognito.md`)
- 対応Step: Step2〜4(ALB導入・NWレイヤー分離・SGの最小権限化)
- レビュー日: 2026-08-14

## 総評

4ファイルまとめて良い出来です。特に、責務分離という一貫した原則(ルートテーブル・SG・DB権限・User Pool)を、コンポーネントごとに機械的に当てはめるのではなく、都度「本当にその境界が必要か」を検証しながら組み立てている点が良いです(サブネット分割基準の矛盾の発見と修正、ALBを分けない理由の言い直し、ElastiCacheの既知のギャップの意図的な保留、など)。数値を伴う主張(NAT/VPCエンドポイントの料金、Cognitoの無料枠)はすべて一次情報で裏を取っており、根拠のない断定がありません。技術用語の使い分け(ルートテーブル/SG/NACLの役割、ALBセッションとCognitoトークンの階層構造)も正確です。

残っている指摘は、いずれも「今すぐ設計をやり直す必要がある」種類のものではなく、構築フェーズで詰めれば良い項目です。設計レビューとしては合格とし、構築フェーズに進んで問題ありません。

## 観点別レビュー

### 設計

- 4ファイルへの分割は、tfstateのレイヤー分割(ネットワーク+SG+ALB)とは独立して「サービス単位」で行っており、ドキュメントとしての読みやすさを優先した判断が一貫している
- `network.md`の通信経路→選定理由の順序入れ替え(サブネット構成を先に説明してから、その上でIGW/NAT/VPCエンドポイントの理由を展開する)は、依存関係を意識した良い構成
- 各ドキュメントで「書くことがない節は削る」判断を繰り返しており(対応Step、実装、わからなかったこと、可用性設計、コスト等)、テンプレートに引きずられず対象の実態に即して構成している

### セキュリティ

- 3階層(Public/Private-NAT/Isolated)のルートテーブル + SG + Cognito User Pool分離 + DB権限分離、と複数レイヤーで多重防御になっている
- VPCエンドポイント採用の理由が「侵害されたECSからの情報持ち出しを、NAT Gatewayの出口を絞ることで抑える」という具体的な脅威モデルに基づいており、説得力がある
- ALBを論理的に1つにする判断で、当初「Cognito認証が侵害時の被害限定の代替になる」としていた誤った理由付けを、「ALBはステートレスで認証情報を持たないため分離の効果がない」という正確な理由に修正できている。これは脅威モデル(外部からの不正アクセス防止 vs 内部からの侵害後の被害限定)を混同しない、という重要な区別ができている証拠
- **抜け(構築フェーズで対応)**: Cognito App Clientのシークレットの保管方法がどこにも書かれていない。Terraform管理下に平文で置かない(Secrets ManagerまたはSSM Parameter Store経由にする)という方針は、実装レビューの際に確認する

### 可用性

- 2AZ構成の理由(ALB/RDS Multi-AZが元々プライマリ+スタンバイの2ノード構成であること)を明示しており、「予算があれば3AZの方が良い」という誤読を避けられている
- NAT Gateway Regional modeの制約(プライベート接続非対応)と、今回それが問題にならない理由の両方が書かれている。トレードオフを両論併記できている
- ALB・Cognitoはフルマネージドサービスであり、可用性設計として書くべき固有の判断がないと判断して節ごと省略した点は妥当

### 運用性

- NAT Gatewayのegress制限を支えるStripe IPプレフィックスリストの同期(EventBridge+Lambda)は、方針レベルで記載しつつ、実装の詳細と着手タイミングを明確に持ち越し事項として区別できている
- ElastiCacheのB/C未分離という既知のギャップを、対応範囲外と自覚した上で意図的に保留しており、記録にも残している。これは「気づいていない」のと「気づいた上で優先度を下げた」の違いが明確でレビューしやすい

### 保守性

- SGをCIDR指定ではなくSG参照にする理由(ECSのIPアドレスがスケーリングで変動するため)が明記されており、将来の変更に強い設計になっている
- NAT Gatewayのegress制限を、SGのルールとして表現する(NAT Gateway自体にはSGを付与できない、という誤解も途中で訂正済み)、という正しい理解に基づいている

### コスト

- NAT Gateway・VPCエンドポイント・Cognitoのいずれも、AWS公式の料金ページを実際に参照して数値を検証しており、見積もりの信頼性が高い
- **改善余地**: 4ファイルそれぞれに個別のコスト記載はあるが、この範囲(ネットワーク+SG+ALB+Cognito)全体の合計金額がどこにもまとまっていない。必須ではないが、`docs/architecture/README.md`か、いずれかのファイルに合計を一言記載すると、全体感が伝わりやすくなる

### パフォーマンス

- VPCエンドポイント経由の通信、ALBのマネージドスケーリングなど、現状の想定トラフィック規模ではボトルネックになりそうな設計判断は見当たらない
- 現時点でパフォーマンス要件が明文化されていないため、この観点は「懸念なし」であって「十分に検証済み」ではない。実トラフィックが発生した後に再評価する対象

### Terraform化した場合の改善点

- SGはSG参照ベースで設計されているため、Terraformでも`aws_security_group_rule`の`source_security_group_id`を使う実装と自然に対応する
- ALBの`session_timeout`・Cognitoのトークン設定は、`aws_lb_listener_rule`の`authenticate_cognito`ブロックのパラメータとして素直にマッピングできる
- backend設定は既存のADR-002方針(部分バックエンド設定、AWSアカウントIDを`.tf`に含めない)を、このレイヤーでも徹底する

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: NAT Gateway/VPCエンドポイントの選定理由が明文化されており、変更理由の追跡がしやすい。監視・アラートの設計はまだ範囲外(後続フェーズ)
- セキュリティ: 多層防御(ルートテーブル・SG・Cognitoプール分離・DB権限)が一貫した原則で設計されている。App Clientシークレットの管理方法のみ未定
- 信頼性: マルチAZ・Regional NAT Gateway・フルマネージドサービス(ALB/Cognito)の活用により、可用性の設計判断は妥当
- パフォーマンス効率: 現状のトラフィック規模を踏まえると過不足のない構成。将来の見直し前提
- コスト最適化: すべての見積もりが一次情報に基づいており、コストとセキュリティのトレードオフ(VPCエンドポイントの追加コストを受け入れる判断など)が明確
- 持続可能性: 直接的な観点での言及はないが、フルマネージドサービス中心の構成により、自前でのリソース過剰プロビジョニングは避けられている

### 実務ならどう設計するか

- ここまでの設計判断のレベル(脅威モデルに基づく理由付け、代替案の明示的な比較、数値の裏取り)は、実務のレビューでもそのまま通用する水準
- 実務であれば、ALBのSSL証明書がどのドメイン名に対して発行されるか(CloudFront VPC Origins経由の内部通信であることを踏まえたドメイン戦略)を、この段階でネットワーク・DNS担当と早めに確定させる。現状は方針止まりで、構築時に決める持ち越し事項になっている
- Cognito App Clientのシークレット管理は、実務では最初のTerraform実装から`aws_secretsmanager_secret`等を組み込むのが一般的

## 理解できていること

- ルートテーブル・SG・NACLそれぞれの役割の違い(特に「local」ルートがVPC内で自動的に全サブネットを接続すること、実際の遮断はSG/NACLの役目であること)
- ALBの`authenticate-cognito`の内部動作(OAuth2認可コードフロー、ALB自身のセッションCookieとCognitoトークンの階層構造)
- NAT Gateway/VPCエンドポイントそれぞれのコスト構造と、選択の技術的トレードオフ

## 曖昧なこと

- ALBのSSL証明書のドメイン名戦略(内部通信用にどう証明書を用意するか)
- Cognito App Clientのシークレットの具体的な保管方法

## 理解できていないこと

- 特になし。会話の中で生じた疑問点(Cognitoの基礎、トークンとセッションの関係、NAT/SGの役割分担の誤解など)は、いずれもその場で解消されている

## 次のアクション

- [ ] `docs/architecture/README.md`のサービス一覧に4ファイルを追加し、状態を「実装レビュー待ち」等に更新する
- [ ] 構築フェーズで、Cognito App Clientシークレットの保管方法(Secrets Manager等)を決定する
- [ ] ALBのHTTPSリスナー・リスナールール(`alb.tf`、パスベースルーティング+Cognito認証アクション)は未着手。`aws_lb_listener`に必須の`certificate_arn`がACM証明書を必要とし、その証明書の検証(DNS validation)にはRoute53のレコード追加が必要なため、Route53/ACM/CloudFrontレイヤー構築時にまとめて着手する(2026-08-15判断。証明書をどのレイヤーに置くかという配置の問題ではなく、検証に必要なRoute53が未構築という根本的なブロッカーのため保留)
- [ ] Terraformのbackend設定は、部分バックエンド設定(ADR-002方針)を踏襲する
- [ ] ElastiCacheのB/C分離は、コンピュートB/Cレイヤーの清書時に別途対応する(既知の持ち越し事項)
- [ ] ALB用SGのInboundルール(CloudFrontからの通信許可)は、CloudFrontレイヤー構築時にCloudFrontが自動生成するサービス管理型SG(`CloudFront-VPCOrigins-Service-SG`)を参照する形で追加する。現時点ではCloudFront自体が未構築のため、暫定的な管理プレフィックスリスト方式では追加しない
- [ ] S3向けGateway型VPCエンドポイント(`vpc_endpoint.tf`の`aws_vpc_endpoint.s3`)に、画像用バケットへアクセスを限定するエンドポイントポリシーを追加する。現時点では画像用バケット自体が別レイヤー(CloudFront/Route53/ACM/S3)で未構築のため、ポリシーなしで作成している

## 2026-08-15 実装レビュー

## レビュー対象

- 対象: `infra/network-sg-alb/`(`provider.tf` / `backend.tf` / `vpc.tf` / `subnet.tf` / `route_table.tf` / `nat_gateway.tf` / `internet_gateway.tf` / `security_group.tf` / `vpc_endpoint.tf` / `alb.tf` / `cognito.tf`)
- 対応Step: Step2〜4(ALB導入・NWレイヤー分離・SGの最小権限化)
- レビュー日: 2026-08-15

## 総評

設計レビューで指摘した内容が漏れなくコードに反映されており、`terraform validate`・`plan`もエラーなし(最終61リソース)で、設計と実装の間にズレがありません。特に良かったのは、実装を進める過程で設計段階では気づいていなかった問題(サブネット分割の矛盾、ElastiCacheの自己参照Inbound漏れ、RDSレプリケーションのSG誤解など)を複数発見し、その都度docs側にも遡って反映できていた点です。実装は設計の単純な変換作業ではなく、設計の理解を深める機会にもなっていました。

指摘は軽微なもの1件のみです。実装レビューとしても合格とし、この範囲でのapply確認に進んで問題ありません。

## 観点別レビュー

### 設計

- `docs/architecture/`側で決めた4ファイルの範囲と、実際に作られたTerraformリソースが1対1で対応しており、設計と実装の乖離がない
- クロスレイヤー依存(CloudFrontのSG、画像用バケットのポリシー、Stripeのプレフィックスリスト、ACM証明書)を、実装時に気づいた時点でその都度`docs/reviews/`に持ち越し事項として記録しており、「後で忘れる」リスクを抑えられている

### セキュリティ

- SG参照ベースのIngress/Egressルールが、要件・前提の内容と過不足なく対応している(ALB↔front/api、front/api↔ElastiCache・RDS・VPCエンドポイント、api→S3のprefix_list参照)
- ElastiCacheの自己参照ルールが、実装時の指摘を経てInbound/Outbound両方揃っている。SGはステートフルだが、自己参照の場合は双方向のルールが必要という理解が正しく反映されている
- RDSの自己参照ルールが無いことについて、実装段階で「AWSが内部で自動的に処理するため不要」という結論に至り、`security-group.md`側も併せて修正できている。設計時点の思い込み(ElastiCacheと同様に必要なはず)を、実装時に検証し直した好例

### 可用性

- ALBはinternal=true・2AZ分のサブネットに配置、NAT GatewayはRegional mode、と設計通りの可用性特性がコードに反映されている
- ターゲットグループのヘルスチェック値(閾値3・間隔30秒・タイムアウト5秒)は、設計時点の合意通り「実運用データが無い段階ではデフォルト水準に留め、後で調整する」という方針に沿ったプレースホルダーとして扱われている

### 運用性

- 保留にした項目(CloudFrontのSG、S3エンドポイントポリシー、Stripeのegress、ALBリスナー、Cognitoシークレット)はすべてコード上にもコメントで明記されており、`docs/reviews/`の記録と二重に追跡できる状態になっている

### 保守性

- **改善余地**: リージョン文字列`"ap-northeast-1"`が、`provider.tf`・`vpc_endpoint.tf`(4箇所)・`security_group.tf`のプレフィックスリスト名など、複数ファイルに直接ハードコードされている。将来リージョンを変更する事態は考えにくいが、変更する場合は複数箇所を漏れなく直す必要がある。`variable "region"`や`data "aws_region" "current"`を1箇所に定義し、他から参照する形にすると保守性が上がる。ただし今回の規模(1リージョン固定が前提の設計)では必須の修正ではなく、任意の改善として扱ってよい
- ターゲットグループ4つの`health_check`ブロックが、値も含めて完全に同一の内容を4回繰り返している。`locals`に共通設定をまとめて`dynamic`ブロックで展開する手もあるが、4個程度の繰り返しに対してその複雑さを持ち込む価値は薄く、今のままで問題ない
- ファイル構成(リソース種別ごとに1ファイル)は、Terraformの「module」機能を使わないシンプルな構成だが、この環境(単一環境、複数箇所へのデプロイを想定しない)には適切な粒度。moduleを持ち込むのは、同じ構成を複数回インスタンス化する必要が出てから で十分

### コスト

- コスト面で新たに追加されたリソース(ALB・Cognito)はいずれもフルマネージドで、設計時点の見積もり通り。実装時点で新たなコスト要因は発生していない

### パフォーマンス

- 実装レベルでの懸念は無し。設計レビュー時点の評価(「懸念なし」であって「十分に検証済みではない」)から変わらない

### Terraform化した場合の改善点

(このセクション自体がTerraform実装のレビューのため、上記「保守性」に統合)

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: 保留事項がコードコメント・レビュー記録の両方に残っており、引き継ぎ・再開が容易な状態
- セキュリティ: 設計レビューで挙げた多層防御が、実際のSGルールとして過不足なく実装されている
- 信頼性: `terraform plan`の結果が設計意図と一致しており、意図しないリソースの過不足がない
- パフォーマンス効率: 変更なし(設計レビュー時点の評価を維持)
- コスト最適化: 変更なし(設計レビュー時点の評価を維持)
- 持続可能性: 変更なし(設計レビュー時点の評価を維持)

### 実務ならどう設計するか

- 今回のように、実装を進めながら設計段階の誤り(RDSの自己参照ルート不要、ElastiCacheの片方向ルール漏れ)に気づき、都度ドキュメント側にも反映する進め方は、実務でも理想的な開発サイクルそのもの。設計を「一度書いたら終わり」にせず、実装の過程で更新し続けられている
- リージョン文字列の重複は、実務であれば早い段階でリファクタリングの対象になる。今回のスコープでは必須ではないが、他レイヤー(コンピュートB/C、データベース等)でも同じパターンが増えてくるようなら、どこかのタイミングで見直す価値がある

## 理解できていること

- SG参照における自己参照ルールの双方向性(Inbound/Outbound両方が必要な場合と、片方で足りる場合の見分け方)
- RDSとElastiCacheで、レプリケーション通信の管轄(AWS管理 vs 顧客管理)が異なる理由
- クロスレイヤー依存が発生した際に、実装を無理に進めず保留し、記録に残すという判断

## 曖昧なこと

- リージョン文字列のハードコードをリファクタリングするかどうかは未決定(任意)

## 理解できていないこと

- 特になし

## 次のアクション

- [ ] `docs/architecture/README.md`のサービス一覧の状態を「実装レビュー待ち」から更新する(この範囲は保留事項を除き実装完了、レビューも合格のため)
- [ ] リージョン文字列のハードコード解消は任意。対応する場合は`variable "region"`等を1箇所に定義する
- [ ] `terraform apply`での動作確認後、確認が終わり次第`terraform destroy`する(既存のガードレール通り)

## 2026-08-20 コンピュート層検討に伴う設計修正

### 経緯

`database.md`側と同じ経緯(詳細は`docs/reviews/database.md`の同日付記載を参照)。フロントのDBアクセスを廃止したことに伴い、`security-group.md`のSG要件・前提のうち、ECS(フロント)→RDSを許可していた記述が不要になった。

### 修正内容

- `security-group.md`: 要件・前提から「ECS(フロント)」のOutbound「RDS」を削除。「RDS」のInboundを「ECS(フロント・API)」→「ECS(API)」に変更
- `infra/network-sg-alb/security_group.tf`: `aws_vpc_security_group_egress_rule.ecs_front_to_rds`・`aws_vpc_security_group_ingress_rule.rds_from_ecs_front`を削除。`ecs_front`・`rds`のSG `description`を実態に合わせて修正

### 補足

この変更はSGルールを1組削除するのみで、他のSGルール(ALB↔front/api、front/api↔ElastiCache・VPCエンドポイント、api↔RDS等)には影響しない。2026-08-14設計レビュー・2026-08-15実装レビューで合格とした判断のうち、この1点のみが対象範囲外だった論点(フロントのDBアクセス方式)に起因する修正であり、再レビューは不要と判断する。

### 判定

上記2点を反映した状態で、引き続き実装完了・合格判定を維持する。

## 2026-09-06 保留事項の棚卸し

2026-08-15実装レビューの「次のアクション」で保留にしていた4項目について、その後の各レイヤー構築で解消済みであることを確認した。

- Cognito App Clientシークレットの保管方法: `infra/network-sg-alb/cognito.tf`でSecrets Manager(`aws_secretsmanager_secret`/`aws_secretsmanager_secret_version`)に格納済み
- ALBのHTTPSリスナー・リスナールール: `infra/network-sg-alb/alb.tf`に、パスパターンの具体性順(`/b/api/*`→`/b/*`→`/c/mypage(booking)/api/*`→`/c/mypage(booking)/*`→`/api/*`)で優先度を設定した`authenticate-cognito`アクション付きリスナールールを実装済み
- ALB用SGのCloudFront Inboundルール: `infra/network-sg-alb/security_group.tf`で、CloudFrontが自動生成するサービス管理型SG(`CloudFront-VPCOrigins-Service-SG`)を参照する形で実装済み
- S3向けGateway型VPCエンドポイントの画像用バケット限定ポリシー: `infra/network-sg-alb/vpc_endpoint.tf`で、画像用バケットARN配下への限定ポリシーを実装済み

上記4項目の解消および下流レイヤー(CloudFront・Route53・ACM・S3・WAF・Stripe IP同期)がすべて完了済みであることを踏まえ、`docs/architecture/README.md`のネットワーク・セキュリティグループ・ALB・Cognitoの状態を「構築中」から「完了」に更新した。

一方、リージョン文字列`"ap-northeast-1"`のハードコード解消は引き続き未対応(任意・低優先度のまま、他レイヤーにも同様のハードコードが残っている)。

## 2026-09-13 リージョン文字列のハードコード解消

178行目「曖昧なこと」・187行目「次のアクション」に記載していた、リージョン文字列のハードコード解消(任意・低優先度)に対応した。

### 対応内容

各state(`waf`・`route53-acm`・`network-sg-alb`・`cloudfront-s3`・`ecr-lifecycle`・`stripe-ip-sync`・`database`・`compute-b`・`compute-c`・`cost-management`の10ディレクトリ)の`provider.tf`に`variable "default_region"`(デフォルト値なし)を追加し、値は`terraform.tfvars`(Git管理外、既存の`tfstate_bucket`と同じ形)から供給する形に統一した。以下の参照箇所もすべて`var.default_region`経由に置き換えた。

- `remote_state.tf`の`terraform_remote_state`データソースの`region`
- `compute-b`/`compute-c`の`ecs.tf`の`awslogs-region`
- `network-sg-alb`の`vpc_endpoint.tf`(VPCエンドポイントのservice name)・`security_group.tf`(S3向けprefix list名)
- `network-sg-alb`の`subnet.tf`(AZ名)。`variable`の`default`値は他の変数を参照できない制約のため、4つのサブネットマッピングは`variable`から`locals`に変更した
- `ecr-lifecycle`・`stripe-ip-sync`の`iam.tf`(Lambda信頼ポリシーのARN)
- GitHub Actions workflow(`front-b`/`front-c`/`api-b`/`api-c`)の`aws-region`。リポジトリのActions Variable `AWS_REGION`を新設して参照する形にした

WAF/ACMの`us_east_1`エイリアスプロバイダは、AWS側の技術的制約(CloudFront用証明書・WAFがus-east-1固定)によるものであり保守性の問題ではないため、意図的にハードコードのまま維持している。

### 確認結果

全10stateで`terraform validate`成功を確認した。`terraform plan`も実行したが、確認時点でAWS上にリソースが存在しない(destroy済み)状態だったため、既存リソースとの実差分は確認できなかった。ただし置き換え後の値(`var.default_region`の実体)は置き換え前のリテラル値と文字列として完全に同一であり、`for_each`のキー等の識別子も変わらないため、既存リソースに対する意図しない`destroy`/再作成は発生しない設計であることをコードレベルで確認した。

### 判定

`fix/20260913/region`ブランチ、PR #36でmainにマージ済み。178行目・187行目の該当項目は解消済み。

## 2026-09-22 設計レビュー(ログイン失敗検知)

## レビュー対象

- 対象サービス: Cognito(`cognito.md`)、ログイン失敗検知(ブルートフォース・クレデンシャルスタッフィング対策)の追加
- 対応Step: ロードマップ外、メンターFB対応
- レビュー日: 2026-09-22

## 総評

良い設計です。特に評価したいのは、「防御(既に備わっている)」と「検知(今回追加する)」を明確に切り分けた点と、その過程で複数回一次情報を確認し直し、思い込みによる設計を都度修正できた点です。Cognito標準の段階的ロックアウト・MFAで防御は既に相当程度成立していること、Plus tireの適応認証はパスワード間違いそのものには反応しないこと、Plus tireに無料枠がなく積み上げ式でもないこと、など、いずれも当初の思い込みを一次情報で覆した上で設計に反映できています。B側のPlus tire導入を一度コストで見送りかけたが、「確率だけでなく影響の大きさも考慮すべき」という指摘を受けて判断を覆した経緯も、このプロジェクトが重視してきたリスクベースの判断として妥当です。

一方、具体的な閾値・検知後の対応手順など、実装に入る前に詰めるべき詳細がまだいくつか残っています。設計レビューとしては合格とし、これらは構築フェーズで詰める前提で進めてよいと判断します。

## 観点別レビュー

### 設計

- 「防御は標準機能で足りている、検知が抜けている」という要件の立て方が明確で、実装項目の必要性がぶれずに説明できている
- アカウント単位の検知(ブルートフォース向け)と全体失敗率の検知(クレデンシャルスタッフィング向け)を、それぞれの攻撃の性質(1アカウント集中/多数アカウント分散)に対応させて使い分けており、片方の指標だけでは他方を検知できない理由も筋道立てて説明できている
- 具体的なアラーム閾値(何件・何%から発火させるか)は未定のまま。構築時に仮値を置き、実運用データで調整する前提であることを明記しておくとよい

### セキュリティ

- `userAuthEvents`ログにユーザー名が含まれるため、検知後に対象アカウントを特定できる設計になっている
- **要確認**: 適応認証(`account_takeover_risk_configuration`)と、C側で既に有効な漏洩認証情報検知(`compromised_credentials_risk_configuration`)は、同じ`aws_cognito_risk_configuration`の別ブロックだが、両方を同時に有効にした場合の組み合わせ動作(判定の優先順位、アクションが競合した場合の挙動)は今回未確認。実装前に一次情報で確認した方がよい
- 検知後の具体的な対応手順(どのケースで`AdminDisableUser`/`AdminResetUserPassword`/`AdminUserGlobalSignOut`のどれを使うか)がまだ整理されていない。検知の仕組みだけでは、実際に運営が動けるかどうかまでは担保できない

### 可用性

- 既存のフルマネージドサービス(Cognito・CloudWatch・SNS)の組み合わせで、可用性設計として特筆すべき懸念はない

### 運用性

- CloudWatch Alarm→SNSという通知経路は、ECRライフサイクル等で既に使ったパターンを踏襲でき、新規性が低く実装リスクも小さい
- アラーム閾値を「仮値からスタートし、実運用データで調整する」方針は、ALBのヘルスチェック値など他レイヤーでも採用してきた進め方と一貫している

### 保守性

- 特筆すべき懸念はない

### コスト

- 数値はAWS公式料金ページで検証済み。当初あった二重計上の誤り(Essentials枠とPlus tireの積み上げ計算)も、一次情報の確認により修正できている
- B側$2+C側$20=$22という最終計算は正しい

### パフォーマンス

- 特筆すべき懸念はない

### Terraform化した場合の改善点

- `aws_cognito_log_delivery_configuration`はこのプロジェクトで初めて扱うリソース。サービスリンクロールやログ配信先のIAM権限で躓く可能性があるため、公式ドキュメントのポリシー例を参照しながら進めるとよい
- CloudWatch AlarmのMetric Math(失敗率の算出)も初めてのパターン。式の構文を確認しながら進める

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: 検知の仕組みは設計できているが、検知後の対応手順(誰が・何を・どの基準で)がまだ明文化されていない
- セキュリティ: 防御と検知を分離した多層防御の考え方が一貫している。適応認証と漏洩認証情報検知の組み合わせ動作の確認が残る
- 信頼性: 影響なし
- パフォーマンス効率: 影響なし
- コスト最適化: 確率だけでなく影響の大きさも踏まえてB側Plus tire導入を判断しており、単純なコスト削減に偏っていない
- 持続可能性: 特筆すべき言及はない

### 実務ならどう設計するか

- 実務であれば、アラーム閾値は初期値を仮置きし、実際の誤検知率・見逃し率を見ながら継続的にチューニングする運用プロセスとして組み込むのが一般的
- 検知後の対応手順は、Runbook(対応手順書)として別途文書化するのが一般的。今回のプロジェクトでも、対応方針が固まった段階で残しておくと良い

## 理解できていること

- Cognito標準の防御機能(段階的ロックアウト・MFA・漏洩認証情報検知)と、Plus tire限定の機能(`userAuthEvents`ログ・適応認証)の違い
- ブルートフォースとクレデンシャルスタッフィングの技術的な違いと、それぞれに適した検知指標の選び方(アカウント単位の失敗数 vs 全体の失敗率)
- Plus tireの料金体系(無料枠なし、Essentials枠との積み上げではなく単体で完結)

## 曖昧なこと

- アラームの具体的な閾値(何件・何%から発火させるか)
- 適応認証と漏洩認証情報検知を同時に有効にした場合の組み合わせ動作

## 理解できていないこと

- 特になし。会話の中で生じた疑問点は都度一次情報で解消できている

## 次のアクション

- [ ] `userAuthEvents`ログ配信設定の実装(B・C両方、CloudWatch Logsへ出力)
- [ ] アカウント単位メトリクスフィルタ+Alarmの実装
- [ ] 全体失敗率Alarm(`SignInSuccesses`のMetric Math)の実装
- [ ] SNS通知経路の実装
- [ ] アラーム閾値の決定(初期値は仮置きとし、実運用データで調整する前提を明記する)
- [ ] 適応認証(`account_takeover_risk_configuration`)と漏洩認証情報検知の組み合わせ動作を、実装前に一次情報で確認する
- [ ] 検知後の対応手順(`AdminDisableUser`等の使い分け)の整理
- [ ] `docs/architecture/README.md`の状態更新(実装完了後)

## 2026-09-23 実装レビュー(ログイン失敗検知)

## レビュー対象

- 対象: `infra/network-sg-alb/cognito.tf`(Plus tire化・ログ配信・`account_takeover_risk_configuration`)・`cloudwatch.tf`(ロググループ・Contributor Insightsルール・Alarm4種)・`sns.tf`(通知経路)
- 対応Step: ロードマップ外、メンターFB対応
- レビュー日: 2026-09-23

## 総評

設計通りの範囲は全て実装され、アカウント単位検知・全体失敗率検知いずれもB・C双方で実機確認(ALARM遷移・SNS通知受信)まで完了しました。実装中に、設計段階では想定していなかった複数の技術的制約(後述)を発見・修正できており、特に「架空のユーザー名への試行はCognitoの認証イベントとして記録されない」という発見は、当初のテスト計画(多数の架空ユーザー名でクレデンシャルスタッフィングを再現する案)そのものを覆す、設計上も重要な学びでした。

最終確認の段階で、設計ドキュメント(`cognito.md`)に記載していた「適応認証を有効にする」が実装リストから漏れていたことが発覚し、追加で実装しました。設計文書の記述が実装計画より先走っていた、という反省点として記録します。実装レビューとしては合格とし、`terraform destroy`・コミット・PR作成に進んで問題ありません。

## 観点別レビュー

### 設計

- Plus tire化・`userAuthEvents`ログ配信・Contributor Insights(アカウント単位)・Metric Math(全体失敗率)・SNS通知、設計時に洗い出した実装項目は全て完了した
- **発見**: 架空の(サインアップしていない)ユーザー名へのログイン試行は、`userAuthEvents`に一切記録されないことが実機確認で判明した。これは当初想定していなかった仕様で、クレデンシャルスタッフィングの実機テスト方法を、実在するテストアカウント複数個を使う方式に変更する必要があった。実際の攻撃でも同じ限界(存在しないアカウントを狙った試行は検知できない)があることを意味し、`cognito.md`への反映を次のアクションに残す
- **反省**: `cognito.md`のセキュリティ設計に書いていた「Cognitoの適応認証を有効にする」が、実装リストの5項目には最初から「検討中・未確定」として計上されており、そのまま着手されずに終盤まで見過ごされていた。設計ドキュメントの記述(断定的)と実装計画の記述(未確定)がズレていたことが原因

### セキュリティ

- アカウント単位のAlarm(`signin-failure-by-user-b/-c`)・全体失敗率のAlarm(`signin-failure-rate-b/-c`)、いずれも実際にOK→ALARM遷移とSNS通知受信を確認済み
- `account_takeover_risk_configuration`(High=BLOCK)は、`describe-risk-configuration`でのデプロイ内容確認までは完了。実際のBLOCK発動は、意図的に高リスクと判定させる手段が無いため未検証のまま
- ユーザーへの直接通知(Cognito `Notify`機能)はSES未導入のため見送り、運営への手動通知で代替する判断とその理由(パスワードが運営に知られるリスクの回避、SESという新規サービス導入のスコープ超過)を`cognito.md`に明記できている

### 可用性

- 特筆すべき懸念はない

### 運用性

- Alarmの評価タイミングに、体感で分〜十分程度のばらつきがあった(Contributor Insightsのデータが後から遡って補正される挙動を実機で確認)。運用時にアラーム遅延として認識しておく必要がある

### 保守性

- 実装中に発見・修正した誤りが多数あった(下記「Terraform化した場合の改善点」に集約)。いずれも`terraform validate`/`apply`のエラーメッセージ、または実際のAWS上の値の突合を通じて発見できており、確認プロセス自体は機能していた

### コスト

- 追加コストの見積もり(Contributor Insights: 月$0.5、Plus tire: 既存の見積もり通り)は設計時点のまま変更なし

### パフォーマンス

- 特筆すべき懸念はない

### Terraform化した場合の改善点

今回発生した実装ミスの多くは、初めて扱うリソース固有のスキーマ誤認によるものだった。

- `aws_cognito_log_delivery_configuration`: ブロック名は単数形`log_configuration`ではなく複数形`log_configurations`が正しい
- `aws_cloudwatch_metric_alarm`(`metric_query`が複数あり、うち1つがexpression専用の場合): そのクエリに`period`の明記が必要(他のクエリから期間を推測できないため)
- `aws_cloudwatch_metric_alarm`の`treat_missing_data`: `metric_query`ブロックの中ではなく、リソース直下の属性
- `SignInSuccesses`メトリクス: `UserPool`だけでなく`UserPoolClient`も同時に指定しないとデータを取得できない(公式ドキュメントに明記があったが見落とした)
- C側の`dimensions`に、コピペミスでB側の`UserPoolClient`を参照していた箇所があった
- `aws_cognito_user_pool_client`: `supported_identity_providers = ["COGNITO"]`が無いとHosted UIのログイン画面自体が機能しない
- Managed Login(`ManagedLoginVersion: 1`のドメイン)は、`aws_cognito_managed_login_branding`(`use_cognito_provided_values = true`で最低限可)が無いとログイン画面がエラーになる
- `account_takeover_risk_configuration`の`low_action`/`medium_action`/`high_action`は、`event_action`に加えて`notify`(真偽値)も必須

いずれも初見では気づきにくい仕様で、次回同様のリソースを扱う際の参考情報として価値が高い

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: 実装中に発見した複数の制約・回避策が、このレビュー記録に集約されており、再現可能な形で残っている
- セキュリティ: 検知経路は実機で動作確認済み。適応認証のBLOCK動作、ユーザーへの直接通知は、それぞれ検証手段の欠如・スコープ判断により未実装/未検証のまま持ち越し
- 信頼性: 影響なし
- パフォーマンス効率: 影響なし
- コスト最適化: 設計時点の見積もりを維持
- 持続可能性: 特筆すべき言及はない

### 実務ならどう設計するか

- 「架空アカウントへの攻撃は検知できない」という今回の発見は、実務であれば重大な指摘事項になる。対策としては、存在確認自体を別の手段(例: WAFのボット対策、レートリミット)で補完する、あるいはこの限界を前提として許容し文書化する、といった選択肢がある
- 設計ドキュメントと実装計画がズレた今回の反省は、実務であれば「設計ドキュメントの記述は、対応する実装チケット/タスクとひもづけて管理する」といった運用でも防げる種類の問題

## 理解できていること

- `userAuthEvents`→Contributor Insights→CloudWatch Alarm→SNSという、一連の検知パイプラインの構成要素とそれぞれの必須設定
- `SignInSuccesses`メトリクスの正しいディメンション指定
- Cognito Managed Loginの必須要件(ブランディング・identity provider)

## 曖昧なこと

- 適応認証(`account_takeover_risk_configuration`)と漏洩認証情報検知(C側)を同時に有効にした場合の優先順位・組み合わせ動作(一次情報でも確証を得られず)
- Contributor Insightsのデータ反映タイミング(後から遡って補正される挙動)の正確な仕様

## 理解できていないこと

- 特になし

## 次のアクション

- [x] 架空のユーザー名への攻撃はCognitoの認証イベントとして記録されない、という制約を`cognito.md`の要件・前提またはセキュリティ設計に明記するか検討する(2026-09-23対応、セキュリティ設計欄に追記。組み合わせ動作の検証・BLOCK動作の検証は検証手段が無いためこれ以上追わない方針、SES導入は任意・低優先度として保留、いずれも[[mentor-review-step2-8]]に記録)
- [ ] 適応認証の実際のBLOCK動作、および漏洩認証情報検知との組み合わせ動作の検証(現時点では検証手段が無く保留)
- [ ] SES導入によるユーザーへの直接通知の実装(見送り中、将来の改善候補として`cognito.md`に記録済み)
- [ ] `terraform destroy`の実施
- [ ] 変更のコミット・PR作成
