# docs/reviews/

設計レビュー・実装レビューの記録。ファイル名は`<対象>.md`(例: `baseline-network-compute.md`)。

1つの対象に対して複数回レビューが発生する場合(設計レビュー→実装レビュー等)は、同じファイルに日付・レビュー種別付きの見出しで追記していく(上書きしない)。

テンプレートは[templates/design-review-template.md](../../templates/design-review-template.md)。
