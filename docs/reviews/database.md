## 2026-08-17 設計レビュー

## レビュー対象

- 対象サービス: データベース(`database.md`)
- 対応Step: tfstate6分割の「データベース」レイヤー(RDSのみ、ElastiCache・バックアップ/DRは対象外)
- レビュー日: 2026-08-17

## 総評

良い出来です。特筆すべきは、ほぼすべての判断が「何を防ぎたいか(脅威モデル)」から逆算されている点です。転送時暗号化を見送った理由(盗聴が成立する経路には既にSG/Isolatedサブネットが効いており、成立するケースではECS侵害が前提になるため対策として機能しない)、保存時暗号化を残した理由(スナップショットがSGを迂回するIAM経路であるという別の脅威モデル)、KMSのデフォルトキーで十分とした理由(復号権限を分離すべき別主体が存在しない)、DBユーザーをB/C×フロント/APIの4分割にした理由(フロントは参照のみ・APIは参照書き込み、という実際の操作内容の違い)、いずれも「なんとなく安全そうだから」ではなく、具体的な攻撃者の立ち位置や操作内容から必要性を検証しています。

コスト試算もAWS Pricing APIで一次情報を確認しており、その過程でストレージ単価がSingle-AZの数字のままになっていた誤りも発見・修正できています。

残っている指摘は、いずれも設計をやり直す必要があるものではなく、構築フェーズで確定させれば良い項目です。設計レビューとしては合格とし、構築フェーズに進んで問題ありません。

## 観点別レビュー

### 設計

- テンプレートの各セクションが、それぞれの役割(要件・前提=何を満たすか、通信経路/セキュリティ設計=何を作るか、選定理由=なぜそれを選んだか、可用性設計=何が起きたら何が守られるか)に沿って過不足なく書き分けられている
- 「選定理由」セクションで、RDS vs Auroraの比較検討をADR-003へのリンクに切り出し、`database.md`側は結論への導線のみに留めた判断は、今回合意した設計ドキュメントとADRの役割分担(何を作ったか vs なぜその決定に至ったか)を体現できている

### セキュリティ

- SG・DBユーザー4分割・Secrets Manager・保存時暗号化・パブリックアクセスブロックの多層防御が、それぞれ独立した脅威(ネットワーク経由の不正アクセス、片方のユーザーによるもう片方のテーブルへの誤操作、認証情報の長期漏洩、スナップショット経由の持ち出し)に対応しており、重複のない設計になっている
- 転送時暗号化を見送った判断は、「同一VPC内だから安全」という短絡的な理由ではなく、「盗聴が成立する具体的なシナリオが、ECSタスク侵害という既により深刻な状態を前提にする」という攻撃者視点の分析に基づいており、説得力がある
- **抜け(構築フェーズで確定)**: RDSの`deletion_protection`(削除保護)についてどこにも触れられていない。有効にすると誤操作によるDB削除を防げる一方、今回のコスト運用ガードレール(確認後は都度`destroy`する)と相性が悪い(destroy前に無効化する手順が要る)。この両立をどうするか、方針を決めておいた方がよい
- **抜け(構築フェーズで確定)**: 自動バックアップの保持期間(`backup_retention_period`)が未確定。要件・前提で「バックアップ・DR設計は対象外」としているが、これはRDSのデフォルト機能(未設定なら保持期間が有効になる)を無効化する意図とは別の話。デフォルトのままにするか、明示的に0にするか、方針が必要

### 可用性

- Multi-AZが対応する障害の粒度(AZ障害・個々のインスタンス障害の両方に自動フェイルオーバーで対応)と、フェイルオーバー時にエンドポイントが変わらない(アプリ側の接続設定変更が不要)ことの両方が、正しい因果関係で書けている
- インスタンスクラスの選定理由の中で「Multi-AZだから後からの変更が低リスク」という関連付けができており、可用性設計とコスト最適化の判断が独立せず補強し合っている

### 運用性

- ADR-003がリンクされているが中身は空のまま。次のアクションとして残っているが、`database.md`側からリンク切れではなく「参照先が未記載」という状態を作ってしまっているので、優先度は上げた方がよい

### 保守性

- 判断の理由がすべて文章として残っており、後から見返した際に「なぜこの構成にしたか」が本人以外にも追える状態になっている。ポートフォリオとしての説明可能性の基準を満たしている

### コスト

- インスタンス・ストレージいずれもAWS Pricing APIで実際の金額を確認しており、見積もりの信頼性が高い。特にストレージ単価がSingle-AZの数字のままだった誤りを、指摘を受けて実際に検証し、正しい数字(Multi-AZ: $0.276/GB/月)に修正できたプロセスが良い
- Secrets Manager側もAPIコール数・シークレット数に基づいた計算ができており、根拠のない概算になっていない
- **改善余地(軽微)**: Secrets ManagerとRDSそれぞれの小計はあるが、このレイヤー全体の合計金額(約$43.67/月)がどこにもまとまっていない。必須ではないが一行あると全体感が伝わりやすい

### パフォーマンス

- 現状のワークロード特性(実トラフィックがほぼ無い)に対して、バーストパフォーマンス型・gp3・Standardモードという構成は過不足がなく、かつ「後から低リスクで変更できる」という前提の上に成り立っている点が一貫している
- バーストモードをStandardにした場合、クレジットを使い切るとベースライン性能まで制限される(追加課金の代わりに性能が制限される)点は、コスト最優先の方針とは整合しているが、実際に負荷が増えた際は「課金が増える」より先に「遅くなる」形で兆候が出ることは意識しておくとよい

### Terraform化した場合の改善点

- 転送時暗号化(TLS強制)を見送ったことで、`require_secure_transport`用のカスタムパラメータグループが不要になり、デフォルトパラメータグループのままで済む。KMSもデフォルトキーのため、これらの判断が実装をシンプルにする方向に効いている
- DBの認証情報(ユーザー名・パスワード)は、`.tfvars`やコード内に直接書かず、`random_password`リソース等で生成した値をSecrets Managerに保存する実装にする(tfstateにパスワードが平文で乗る点は、ADR-002のstate管理方針と合わせて意識する)
- `network-sg-alb`レイヤーの`db`サブネットID・`rds-sg`のIDは`terraform_remote_state`で参照する実装になる想定

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: 判断理由の記録は十分。ADR-003が空のままなのが唯一の抜け
- セキュリティ: 多層防御が脅威モデルベースで一貫している。`deletion_protection`・バックアップ保持期間のみ未確定
- 信頼性: Multi-AZ・自動フェイルオーバー・ストレージオートスケーリングの組み合わせが妥当
- パフォーマンス効率: 現状のワークロードに対して過不足のないサイジングで、見直しの容易性も確保されている
- コスト最適化: 一次情報に基づく見積もり、セキュリティとコストのトレードオフが明示的(KMS・TLS双方)
- 持続可能性: 直接の言及はないが、最小サイジング+オートスケーリングにより過剰プロビジョニングを避けられている

### 実務ならどう設計するか

- ここまでの判断の質(攻撃者視点での脅威モデル分析、一次情報での価格検証、代替案の比較と却下理由の明記)は実務のレビューでもそのまま通用する水準
- 実務であれば、`deletion_protection`と`backup_retention_period`はRDS構築時に必ず明示的に決める項目で、デフォルト任せにはしない。今回の唯一の実質的な指摘はここ

## 理解できていること

- KMSの本質的な価値(復号権限を別主体に分離できること)と、その価値を必要としない場合はデフォルトキーで十分という判断基準
- 転送時暗号化が防ぐ対象(通信経路上の盗聴)と、ネットワーク分離・SGが防ぐ対象の違い、両者の守備範囲が重ならない場合は前者の追加コストに見合わないという判断
- RDSのスナップショットがSG・VPCを経由しないIAM/API操作である、という保存時暗号化の必要性の根拠
- ストレージとコンピュートが分離しているためインスタンスクラス変更がデータに影響しないこと、Multi-AZ構成がその変更のダウンタイムも最小化すること
- 最小権限の原則を、B/Cの区分だけでなくフロント/APIの実際の操作内容の違いにも一貫して適用できたこと

## 曖昧なこと

- 自動バックアップの保持期間をどうするか(デフォルトのままか、明示的に0にするか)
- `deletion_protection`を有効にするかどうか、コストガードレール(確認後destroy)との運用上の両立方法

## 理解できていないこと

- 特になし。会話中に生じた疑問(RPO/RTOの扱い、暗号化の脅威モデル、インスタンスタイプの選び方、Multi-AZ変更の安全性など)は、いずれも本人が咀嚼した上で正しい結論に至っている

## 次のアクション

- [x] `docs/adr/003-rds-vs-aurora-selection.md`の中身(コンテキスト・選択肢・決定・理由)を記載する
- [ ] KMSのデフォルトキー選定もADRの候補(ADR-003に含めるか別立てにするかは本人判断、任意・見送り)
- [x] `deletion_protection`・`backup_retention_period`の方針を決め、`database.md`に追記する(Terraform実装時に決定してよい)
- [x] `docs/architecture/README.md`のサービス一覧に「データベース」行を追加する
- [x] Terraform実装(`infra/database/`想定)に着手。`network-sg-alb`レイヤーの`db`サブネット・`rds-sg`を`terraform_remote_state`で参照する
- [x] コストの見積もりに、Secrets Manager+RDSの合計金額を一行追加する(任意、$44.07/月として反映済み)

## 2026-08-17 ADR-003確認

`docs/adr/003-rds-vs-aurora-selection.md`の内容を確認。RPO/RTOが未決定であることを正しく「根拠として弱い」と表現し、Aurora優位な技術的論点(リードレプリカの共有ストレージ有無)も自ら認めた上でコスト・現状トラフィックを理由に見送る、という構成になっており、単純な結論ありきの理由付けになっていない。検討の主体を自分自身に統一した表現になっており、スコープ外の論点(目新しさ・市場シェア)も含めず、公開ADRとして問題ない内容。

残る`deletion_protection`・`backup_retention_period`の方針は、設計をやり直す必要のある項目ではないため、Terraform実装と並行して決定・追記してよい。この時点で**設計フェーズは完了とし、Terraform実装に進んで問題ない**。

## 2026-08-17 実装レビュー

## レビュー対象

- 対象: `infra/database/`(`provider.tf` / `backend.tf` / `backend.hcl` / `remote_state.tf` / `rds.tf` / `secrets_manager.tf` / `terraform.tfvars`)、および参照元`infra/network-sg-alb/outputs.tf`の追加
- 対応Step: tfstate6分割の「データベース」レイヤー
- レビュー日: 2026-08-17

## 総評

`terraform plan`が14リソースの作成でエラーなく完了し、内容も設計(`database.md`)と一致しています。実装中に見つかったバグ(`subnet_ids`の二重リスト化、`db_subnet_group_name`の参照誤り、`engine_version`の無効な値、`locals.xxx`という誤った参照構文、`for_each`への`list`直接渡し)はいずれもその場で修正されました。特に良かったのは、単なるエラー解消で終わらせず、「この値は外部から変更する想定か」という観点から`variable`を`locals`に切り替えた点、マスターユーザーのパスワード管理を、当初案(`random_password`+手動Secrets Manager)から`manage_master_user_password`(AWS管理、stateに平文パスワードが一切乗らない)に切り替えた点です。いずれも単に動くようにする以上の、設計の質を上げる修正になっています。

1点、コスト見積もりの更新漏れがあります。それ以外は実装レビューとして合格です。

## 観点別レビュー

### 設計

- `database.md`で決めたスコープ(RDS本体+認証情報の保管まで、DBユーザー作成はCI/CD側)通りに実装されており、範囲の逸脱がない
- `network-sg-alb`側への依存(`db`サブネット・`rds-sg`)を`terraform_remote_state`経由で正しく解決できている

### セキュリティ

- マスターユーザーは`manage_master_user_password = true`により、パスワードがTerraformのstateに一切乗らない形で実装できている。当初提示した案(`random_password`を使う方法)よりも安全な選択肢に自ら切り替えられた
- アプリ用4ユーザーの認証情報生成・保存が`for_each`で過不足なくカバーされている
- `random_password`の`override_special`を、DB接続文字列で問題を起こしやすい記号を除外する形に絞り込めており、実務的な配慮ができている

### 可用性

- `multi_az = true`など、設計通り。変更なし

### 運用性

- DBユーザー作成(GRANT)をCI/CD側に切り出す判断と、その経緯が記録されており、セッションをまたいでも追跡できる状態になっている

### 保守性

- ファイル分割(リソース種別ごと)・`for_each`の活用など、`network-sg-alb`レイヤーと一貫したスタイルで書けている
- **改善余地**: `deletion_protection`・`backup_retention_period`の決定理由はコード側にコメントとして残っているが、`database.md`側への反映がまだ確認できていない(前回レビューの次のアクションのまま)

### コスト

- **抜け**: `manage_master_user_password = true`により、AWS側でマスターユーザー専用のSecrets Managerシークレットが自動的に1つ追加される。これは`plan`結果には明示的なリソースとして現れない(AWS内部で暗黙的に作成されるため)が、実際には課金対象。`database.md`のコスト見積もりはアプリ用4シークレットのみで計算されており、**マスター用の5シークレット目が反映されていない**

### パフォーマンス

- 変更なし

### Terraform化した場合の改善点

- 全体として大きな改善提案は無し。実装を進める中で見つかった誤りへの対応が、いずれも「なぜその書き方がより適切か」まで踏み込めており、単純なtypo修正のレベルを超えている

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: `plan`結果と設計ドキュメントの記述が一致。セッションをまたぐ持ち越し事項も記録済み
- セキュリティ: `manage_master_user_password`の採用により、設計レビュー時点より安全な実装に到達
- 信頼性: 変更なし
- パフォーマンス効率: 変更なし
- コスト最適化: 概ね妥当だが、マスターユーザー用シークレットの見積もり反映が未了
- 持続可能性: 変更なし

### 実務ならどう設計するか

- `manage_master_user_password`のような、Terraformが暗黙的に作成する付随リソース(secret、レプリカ等)はコスト見積もりから漏れやすく、実務のレビューでも定番の見落としポイント。今回のように、実際に`plan`を通してから見積もりを見直す習慣は良い実践

## 理解できていること

- `terraform_remote_state`が「今の`.tf`ファイル」ではなく「最後に`apply`されたstate」を読む、という仕組み(実際にエラーを経験し、`network-sg-alb`側で`apply`することで解決した)
- `variable`(外部からの入力)と`locals`(内部の固定値)の使い分けの意図
- `for_each`が`map`/`set`を要求する理由と、`list`→`set`への変換が必要な場面
- マスターユーザーとアプリ用ユーザーの役割の違い(ブートストラップ用 vs 実際にアプリが使う最小権限アカウント)

## 曖昧なこと

- マスターユーザー用のSecrets Managerシークレットのコストが、`database.md`の見積もりにまだ反映されていない

## 理解できていないこと

- 特になし

## 次のアクション

- [x] `database.md`のコスト見積もりに、マスターユーザー用シークレット(5個目)分を追加する
- [x] `deletion_protection`・`backup_retention_period`の決定内容と理由を`database.md`のセキュリティ設計・可用性設計に反映する(前回からの持ち越し)
- [x] 実際に`terraform apply`して動作確認するかどうかを判断する。`apply`する場合は、確認後に必ず`terraform destroy`する(コストガードレール通り)
- [ ] CI/CD側(新チャット2)でのDBユーザー作成タスクとの接続(Secrets Manager ARNの受け渡し方法など)は、このレイヤーの範囲外のためCI/CD側の設計時に詰める

## 2026-08-18 apply/destroy確認・マージ準備

`terraform apply`で実機確認を実施。RDS(`available`、MySQL 8.0.46、`db.t4g.micro`、Multi-AZ、`gp3`20GB、暗号化・パブリックアクセスブロック・`deletion_protection=false`・`backup_retention_period=0`いずれも設計通り)、Secrets Manager(アプリ用4つ+マスター用1つ、マスター用はローテーションも自動有効)を実機で確認。`database.md`との齟齬なし。

確認後`terraform destroy`を実行。`skip_final_snapshot`未設定によるエラー、および一度目のdestroyで削除したSecrets Managerシークレットが30日の復旧猶予(`recovery_window_in_days`のデフォルト)で残り再作成に失敗する問題が発生したが、いずれも原因を特定し解消(`skip_final_snapshot = true`の追加、`recovery_window_in_days = 0`の追加、および残存シークレットの`--force-delete-without-recovery`)。最終的にRDSインスタンス・Secrets Manager・NAT Gateway・ALB・非デフォルトVPCいずれも0件であることをAWS CLIで確認済み。

**データベースレイヤーはこれで完了とし、mainへのマージに進んで問題ありません。**

## 2026-09-01 設計レビュー(Step9: バックアップ・DR)

## レビュー対象

- 対象サービス: データベース(`database.md`の要件・前提/RPO・RTO・スナップショット保持期間セクション/可用性設計/コスト見積もり)、[ADR-006](../adr/006-multi-region-dr-scope.md)
- 対応Step: Step9(バックアップ・DR、RPO/RTO)
- レビュー日: 2026-09-01

## 総評

良い出来です。今回の核心は「Multi-AZの同期レプリケーションは人為的ミスによる破損を防げない(誤った書き込みも両AZに伝播する)」という気づきに自力で到達し、そこから「AZ障害」「人為的ミスによる論理的破損」「リージョン障害」という3種類の異なる脅威に、それぞれ別の対策(Multi-AZ自動フェイルオーバー/PITR・自動バックアップ/オフライン業務継続)を対応させる、という筋の通った設計に落とし込めた点です。

RPO=5分・RTO=1時間という数値も、「なんとなく」ではなく、RDSの技術的なデフォルト粒度・手動復元の作業内訳から逆算しており、かつ「本来はビジネス影響分析(BIA)で決めるべきで、これは暫定値である」という誠実な留保が明記できています。マルチリージョンDR不採用(ADR-006)も、コスト・現状トラフィックに基づく判断に加えて、将来の移行パス(既存リージョン側は変更小、新リージョン側は別途構築)まで見据えられており、RDS vs Aurora(ADR-003)からの判断の一貫性を保てています。

設計をやり直す必要のある指摘はありません。構築フェーズに進んで問題ありません。

## 観点別レビュー

### 設計

- 要件・前提(RPO/RTOの数値)、設計(RPO/RTO/保持期間セクションでの根拠、可用性設計での結論)、ADR(比較検討)という役割分担が、既存の`database.md`の構成(選定理由⇔ADR-003と同じ形)に沿って一貫している
- 「今回実装する範囲(AZ障害・人為的ミス)」と「ドキュメントに留める範囲(リージョン障害)」の線引きが、19行目に明記されており、読み手が設計の対象外を誤解しない作りになっている

### セキュリティ

- 前回レビュー(2026-08-17)からの変更なし。新たな懸念も発生していない

### 可用性

- Multi-AZ(AZ障害)・PITR(人為的ミス)・オフライン業務継続(リージョン障害)の3層が、それぞれ異なる障害の性質(同期レプリケーションの伝播範囲、技術的な復旧粒度、AWSインフラ全体への依存有無)に基づいて選び分けられている。REL 9・REL 13の両方に対応する設計として妥当
- **軽微な抜け**: 30行目「そもそも、障害をどのように検知するかは、今後検討する」は、正直に持ち越しとして明記できている点は良いが、RTO=1時間の内訳(検知・判断+復元操作)のうち検知の部分が未設計のままだと、実測時にRTOの妥当性を正しく検証できない。監視・ログ設計フェーズへの持ち越しとして`次のアクション`に明記しておくとよい

### 運用性

- ADR-006が、単なる「決定した」ではなく「なぜ他の選択肢を採らなかったか」まで含めて書けており、後から見返しても意思決定の背景が追える
- オフライン業務継続の実現方法(会員登録完了メール/予約完了メールへの電話番号記載)が、システム側の実装(メール送信処理へのテキスト追加程度)として具体的にイメージできるレベルまで詰められている

### 保守性

- RPO/RTOの数値だけでなく「本来はこう決めるべき」という一般論も併記されており、後から読み返した際に、暫定値と原則を混同しないようになっている

### コスト

- バックアップストレージの無料枠(プロビジョニング済みストレージ量と同量、今回は20GB)を一次情報で確認した上で試算しており、前回レビュー時と同じ水準の丁寧さを維持できている
- マルチリージョンDRのコスト(クロスリージョンレプリケーション・常時稼働リソース)を定量化はしていないが、「見送り」の判断自体には現時点で定量化するほどの精度は不要と判断でき、致命的な抜けではない

### パフォーマンス

- 変更なし

### Terraform化した場合の改善点

- `backup_retention_period`は現在`0`(2026-08-18実機確認時点)になっているため、`7`への変更が今回のTerraform実装の主な差分になる
- バックアップウィンドウ(`preferred_backup_window`)・メンテナンスウィンドウとの重複回避は、設計ドキュメントには明記されていないが、`deletion_protection`同様「構築フェーズで確定してよい」項目として扱ってよい

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: 判断の根拠・持ち越し事項(検知手法)の両方が明記されている
- セキュリティ: 変更なし
- 信頼性: REL 9(バックアップ)・REL 13(DR計画)の両方に、脅威ごとに異なる対策を対応させられており、このステップの主目的を満たしている
- パフォーマンス効率: 変更なし
- コスト最適化: マルチリージョンDRの投資対効果を、現状トラフィックに基づき定性的に判断できている
- 持続可能性: 直接の言及はないが、過剰投資(常時稼働のDR環境)を避けている点で間接的に整合

### 実務ならどう設計するか

- 実務であれば、RPO/RTOは正式なBIAプロセス(事業部門・カスタマーサポート等のステークホルダーを巻き込む)で決定し、技術的なデフォルト値だけでは正当化しない。今回「本来はこう決めるべき」という理想像を明記しつつ、現状は技術的デフォルト値で代替した、という誠実な書き方は、実務のレビューでも好意的に評価される部類
- 障害検知は、実務ではCloudWatch Alarmやアプリケーション側の整合性チェックによる自動検知が前提になることが多い。「今後検討する」という現状の記載は、監視設計フェーズが実際に来た際に具体化すべき項目として適切に切り分けられている

## 理解できていること

- Multi-AZの同期レプリケーションが、AZ障害には有効だが人為的ミス・アプリバグによる論理的破損には無力である(誤った書き込みも両AZに伝播する)という区別
- RPOとRTOの違い(データ損失の許容量 vs 復旧までの時間)、およびそれぞれの技術的な実現手段(PITR vs 手動復元オペレーション)
- マルチリージョンDRの実現パターンの違い(常時スタンバイ方式 vs Backup and Restore方式)と、それぞれのコスト構造の違い
- オフライン業務継続における「提携先施設への直接連絡」と「自社サポート窓口」の役割の違い(予約可否の判断ができるのは施設側自身であるため)

## 曖昧なこと

- 障害検知の具体的な方法(本人も「今後検討する」と明記した通り、現時点では未設計)
- RTO=1時間という数値の実測での妥当性(まだ実際にスナップショット復元テストを行っていない)

## 理解できていないこと

- 特になし。会話中に生じた疑問(RPO/RTOの決め方、Multi-AZの守備範囲、マルチリージョンDRのパターンの違い、オフライン業務継続の実効性)は、いずれも本人が咀嚼した上で正しい結論に至っている

## 次のアクション

- [ ] Terraform実装(`infra/database/`の`backup_retention_period`を`0`→`7`に変更、バックアップウィンドウの要否を決定して`apply`)
- [ ] 実際に手動スナップショットを取得し、新規RDSインスタンスへの復元テストを行う(RTO=1時間の妥当性を実測で検証)
- [ ] 障害検知の具体的な方法は、監視・ログ設計の後続フェーズで改めて検討する(持ち越し事項として記録)
- [ ] 実装完了まで、`docs/architecture/README.md`のデータベース行の状態を一時的に「構築中」に戻すか検討する(現状「完了」のままだと、`backup_retention_period`変更前の状態と食い違う)
- [x] 実装レビュー後、`docs/architecture/README.md`を「完了」に戻す

## 2026-09-02 実装レビュー(Step9: バックアップ・DR)

## レビュー対象

- 対象: `infra/database/rds.tf`(`backup_retention_period`・`backup_window`・`maintenance_window`)、実機でのスナップショット取得・復元テスト
- 対応Step: Step9(バックアップ・DR、RPO/RTO)
- レビュー日: 2026-09-02

## 総評

`terraform apply`が設計通りに完了し、実際にスナップショット取得→別インスタンスへの復元→削除まで一通り実機で経験できました。復元の実測時間(約6分40秒)は、設計時に置いたRTO=1時間という数値に対して十分な余裕があることを裏付けており、`database.md`にも実測値として反映済みです。実装中に2つの誤りが見つかりましたが、いずれも実害が出る前に修正できています。

設計をやり直す必要がある指摘はありません。Step9はこれで完了として問題ありません。

## 観点別レビュー

### 設計

- `database.md`(設計)と実際の`rds.tf`(`backup_retention_period = 7`、`backup_window = "18:30-19:00"`、`maintenance_window = "mon:17:30-mon:18:00"`)が一致している

### セキュリティ

- 変更なし

### 可用性

- 実測復元時間(約7分)がRTO想定(1時間)を大きく下回ることを確認できた。これは「検知・判断」の時間を含まない、純粋な復元操作のみの時間である点も`database.md`の記載と整合している
- 復元テストの手順(スナップショット取得→別識別子での復元→動作確認相当の確認→削除)自体が、実際に障害対応が発生した場合の一次対応の練習になっている

### 運用性

- 実装中に発生した2つのエラー(後述)が、いずれもその場で原因を特定し解決できている。特にSecrets Managerの復旧猶予期間の問題は、2026-08-18に一度経験済みの内容を再現・再解決できており、過去の経験が実際に活きている

### 保守性

- コード側のコメント(`backup_retention_period`が「今後決める」から実際の決定値に更新されている)が、設計ドキュメントの記述と食い違わない状態を保てている

### コスト

- 復元テスト用の一時インスタンス(Single-AZ、`db.t4g.micro`)は数十分の稼働で削除しており、コストガードレール通り実害のない範囲に収まっている

### パフォーマンス

- 変更なし

### Terraform化した場合の改善点

- **見つかった誤り1**: Terraformの`aws_db_instance`リソースの引数名は`preferred_backup_window`ではなく`backup_window`(AWS API名`PreferredBackupWindow`とTerraform引数名が異なる)。`terraform plan`時の`Unsupported argument`エラーで発覚し、その場で修正
- **見つかった誤り2**: `maintenance_window`の値には曜日プレフィックス(`ddd:hh24:mi-ddd:hh24:mi`)が必須(`preferred_backup_window`の書式`hh24:mi-hh24:mi`と異なる)。レビュー時点で指摘し修正済み
- 復元テストは`aws_db_instance`リソースの管理下ではなくAWS CLIで直接行った(Terraform管理外の一時リソース)。これは意図した設計で、本番のRDS本体の設定・状態に影響を与えない実施方法として適切

### AWS Well-Architected Framework 6本柱

- 運用上の優秀性: 実際に手を動かして得た実測値(復元時間)を設計ドキュメントに反映するサイクルができている
- セキュリティ: 変更なし
- 信頼性: REL 9(バックアップの実効性)を、机上の設計だけでなく実機検証まで完了させた
- パフォーマンス効率: 変更なし
- コスト最適化: 復元テストのリソースをSingle-AZ・短時間稼働に留め、検証目的に対して過不足のないコストで実施できている
- 持続可能性: 変更なし

### 実務ならどう設計するか

- 実務では、このような復元テストを一度きりで終わらせず、定期的に(四半期に1回等)実施してRTOの実測値が劣化していないかを継続的に監視する。`database.md`の33行目に本人自身がこの観点(データ量増加に伴う復元時間の増加)を明記できており、実務的な視点が身についている

## 理解できていること

- Terraformの引数名とAWS API名が一致しない場合があり、エラーメッセージ(`Unsupported argument`)から実際のスキーマを確認する必要があること
- `preferred_backup_window`と`preferred_maintenance_window`(Terraformでは`backup_window`/`maintenance_window`)の書式の違い(曜日プレフィックスの有無)
- Secrets Managerの復旧猶予期間(デフォルト30日)により、削除直後の同名シークレット再作成がブロックされる仕様と、`--force-delete-without-recovery`による解消方法
- 手動スナップショット取得から別インスタンスへの復元までの一連の実務手順(AWS CLI)

## 曖昧なこと

- 特になし

## 理解できていないこと

- 特になし

## 次のアクション

- [x] `database.md`のRTOセクションに実測値(約7分)を反映する
- [x] `infra/database`・`infra/network-sg-alb`ともに`terraform destroy`で後片付け、AWS CLIで0件を確認済み
- [ ] `docs/architecture/README.md`のデータベース行は「完了」のまま据え置く(状態変更不要)
- [ ] 障害検知の具体的な方法は、引き続き監視・ログ設計の後続フェーズへの持ち越し事項とする

**Step9(バックアップ・DR)はこれで完了です。**

## 2026-08-20 コンピュート層検討に伴う設計修正

### 経緯

コンピュート層(`compute.md`)の要件・前提を検討する過程で、「フロントコンテナがDBに直接アクセスするか」という、`database.md`設計時点では明示的に検討されていなかった論点が浮上した。B/C分離の要である行単位のデータ隔離(GRANTでは実現できず、アプリケーション側のWHERE句に依存)を守る主体を1箇所(API)に集約すること、また各サービスが独立してデプロイされる構成(front/API間に共通コードが無い)でDBアクセスロジックを二重に持たせるべきではないことから、**フロントのDBアクセスを廃止し、APIのみがDBにアクセスする**方針に変更した。

### 修正内容

- `database.md`: 要件・前提/通信経路/セキュリティ設計から「ECS(フロント)」のDBアクセスに関する記述を削除。DBユーザーを4種類(front-b/front-c/api-b/api-c)→2種類(api-b/api-c)に変更。Secrets Managerのコスト見積もりを再計算($44.07/月 → $43.27/月)
- `infra/database/secrets_manager.tf`: `local.db_users`を`["b_front", "b_api", "c_front", "c_api"]`→`["b_api", "c_api"]`に修正
- 関連するSG側の修正(`security-group.md`・`infra/network-sg-alb/security_group.tf`)は`docs/reviews/network-sg-alb-cognito.md`側に同日付で記録

### 判定

2026-08-17時点の設計・実装レビューの合格判定を覆すものではない(RDS本体・暗号化・可用性設計などのコア判断は変更なし)。DBユーザー数・アクセス元というスコープの狭い修正であり、再レビューは不要と判断する。この修正を反映した状態で、引き続き完了扱いとする。
