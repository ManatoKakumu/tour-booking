name: api-c

on:
  pull_request:
    branches: [main]
    paths:
      - "apps/api-c/**"
  push:
    branches: [main]
    paths:
      - "apps/api-c/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  ECR_REPOSITORY: api-c

jobs:
  build-check:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build image (no push)
        run: |
          docker build -t ${{ env.ECR_REPOSITORY }}:${{ github.sha }} apps/api-c

  push:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: api-c
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/api-c-ecr-push
          aws-region: ap-northeast-1

      - name: Login to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push image
        run: |
          docker build -t ${{ steps.ecr-login.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }} apps/api-c
          docker push ${{ steps.ecr-login.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}

      - name: Download current task definition
        run: |
          aws ecs describe-task-definition --task-definition api-c --query taskDefinition > task-definition.json

      - name: Render new task definition
        id: render
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: task-definition.json
          container-name: api-c
          image: ${{ steps.ecr-login.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}

      - name: Deploy to ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.render.outputs.task-definition }}
          service: api-c
          cluster: tour-booking-c
          wait-for-service-stability: true

  create-db-user:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    environment: api-c
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/api-c-ecr-push
          aws-region: ap-northeast-1

      - name: Run DB user setup task
        run: |
          NETWORK_CONFIG=$(aws ecs describe-services \
            --cluster tour-booking-c \
            --services api-c \
            --query 'services[0].networkConfiguration' \
            --output json)

          TASK_ARN=$(aws ecs run-task \
            --cluster tour-booking-c \
            --task-definition db-user-setup-c \
            --launch-type FARGATE \
            --network-configuration "$NETWORK_CONFIG" \
            --query 'tasks[0].taskArn' --output text)

          aws ecs wait tasks-stopped --cluster tour-booking-c --tasks "$TASK_ARN"

          EXIT_CODE=$(aws ecs describe-tasks \
            --cluster tour-booking-c \
            --tasks "$TASK_ARN" \
            --query 'tasks[0].containers[0].exitCode' --output text)

          if [ "$EXIT_CODE" != "0" ]; then
            echo "DB user setup task failed with exit code $EXIT_CODE"
            exit 1
          fi
